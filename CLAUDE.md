# Shakespeare Portal — Agent Guide

You are working on a Shakespeare reading portal for students. The defining feature is **explaining what students don't already know**: archaic vocabulary, biblical allusions, classical mythology, historical context for the history plays, sexual puns, rhetorical devices, and topical Elizabethan references. Plain reading apps already exist (MIT, OSS, Folger); this product's differentiator is *layered, source-cited, depth-adjustable annotation*.

## Audience

- **Primary**: High-school students (grades 9–12) and early-college undergraduates encountering Shakespeare in a class.
- **Secondary**: Self-directed adult learners returning to the canon.
- **Assume no priors**: zero biblical literacy, no classical mythology, no Tudor history, no Elizabethan slang. Annotate as if the reader is meeting Shakespeare for the first time.
- **Don't condescend.** Concise, neutral, source-cited prose. Treat the reader as a smart person who simply hasn't met this material before.

## Repo Map

| Path | Read this when… |
|---|---|
| **[docs/BUILD_A_PLAY.md](docs/BUILD_A_PLAY.md)** | **You're building a play end-to-end — the autonomous runbook. START HERE.** |
| [README.md](README.md) | You want the human-facing overview |
| [docs/ARCHITECTURE.md](docs/ARCHITECTURE.md) | You need to understand how pieces connect |
| [docs/CONTENT_STANDARDS.md](docs/CONTENT_STANDARDS.md) | You're writing or reviewing annotation prose |
| [docs/ANNOTATION_SCHEMA.md](docs/ANNOTATION_SCHEMA.md) | You're emitting or parsing annotation JSON |
| [docs/METADATA_SCHEMA.md](docs/METADATA_SCHEMA.md) | You're adding or auditing play-level catalog fields |
| [docs/PIPELINE.md](docs/PIPELINE.md) | You want the conceptual five-stage pipeline (the runbook is the how-to) |
| [docs/UX_PRINCIPLES.md](docs/UX_PRINCIPLES.md) | You're designing or changing the reader UI |
| [docs/LICENSING.md](docs/LICENSING.md) | You're adding a new source or releasing content |
| [schemas/](schemas/) | You need the authoritative JSON Schema for any data file |
| [data/](data/) | The source of truth for all content — read but don't hand-edit annotations |
| [scripts/pipeline/](scripts/pipeline/) | The build kit: `extract-speakers`, `render-scenes`, `postprocess`, `audit` (`.mjs`, run from repo root with bare `node`) + Workflow templates in `workflows/` |
| [scripts/](scripts/) | The lower-level pipeline (TypeScript via tsx — run from inside `scripts/`: `node --import tsx <x>.ts`) |
| [site/](site/) | The Astro app that renders the portal (100% data-driven; no edits needed to add a play) |
| [shakespeare-material-master/](shakespeare-material-master/) | Vendored "Open Shakespeare" source repo — **READ-ONLY** |

## Key Conventions

- **Through-Line Numbers (TLN)** — the scholarly standard from Hinman (1968), used by Folger and ISE. Every line in a play has one continuous integer across the whole play (not act/scene-relative). All annotations cite TLN, not act.scene.line.
- **Play slugs** — lowercase, underscore-separated (`hamlet`, `henry_iv_part_1`, `midsummer_nights_dream`). Match the slugs in `shakespeare-material-master/texts/works.json`.
- **Annotation IDs** — `<play_slug>-<tln_start>-<short_kebab>` (e.g., `hamlet-723-fishmonger`). Stable across rebuilds; never reuse.
- **Word offsets** — 0-indexed, inclusive on both ends. `word_start: 0, word_end: 0` is the first word on the line.
- **Depth tiers** — `basic` (shown by default for novice readers) vs `scholar` (off by default, surfaces textual variants, deep bawdy, etc.).
- **Confidence** — `high` (multi-source, uncontroversial), `medium` (defensible but contested), `uncertain` (excluded from default depth — surface to humans for review).
- **No emoji** in code, content, or docs unless explicitly requested.

## Building a play (the one workflow that matters)

**To build a whole play, follow [docs/BUILD_A_PLAY.md](docs/BUILD_A_PLAY.md) step by step.** It is the
autonomous runbook — written so this session can start from "Read CLAUDE.md. Let's build `<play>`." and
run to a shipping, validated reader with minimal intervention. The 11 steps in brief:

1. Confirm the vendored source + catalog stub exist (Step 0).
2. **Author `data/plays/<slug>/characters.json` first** (`node scripts/pipeline/extract-speakers.mjs <slug>` to discover labels) — the parser canonicalizes speakers against its aliases.
3. Ingest: `cd scripts ; node --import tsx ingest/parse-gutenberg.ts <slug>` then `… ingest/normalize-tln.ts <slug>`. Verify Act 1 Scene 1 vs MIT/Folger.
4. Upgrade the `data/catalog/works.json` stub to shipping form; author `synopsis.json`.
5. **Author reference cards first** (append to `data/references/{classical,historical,biblical}.json`) — a link to a missing card fails citation-check.
6. `node scripts/pipeline/render-scenes.mjs <slug>` (writes per-scene text, prints the `scenes` arg).
7. Generate + fact-check via the **Workflow tool**: `scripts/pipeline/workflows/annotate.mjs`.
8. Merge deterministically: `node scripts/pipeline/postprocess.mjs <slug> --reset` (resolve-anchors + 2-of-3 gate → `annotations.json` / `_review_queue/`).
9. Glossary via Workflow `scripts/pipeline/workflows/glossary.mjs`, then `cd scripts ; node --import tsx generate/merge-glossary.ts <slug> <actCount>`.
10. `cd scripts ; node --import tsx build-index.ts`, then `node scripts/pipeline/audit.mjs <slug>` (stats + all 3 validators; must exit 0).
11. Browser-verify with the preview tools; then update this file's Project Status, the README count, and memory.

**Critical environment facts** (full list in the runbook): the OS is **Windows** — in PowerShell use `;` not `&&`, and never pipe `node`/`git`/`npm` to `Select-Object` (broken pipe → exit 255 → cancels parallel sibling calls). The build-kit `.mjs` scripts run from the **repo root** with bare `node`; the `.ts` scripts run **only from inside `scripts/`** via `node --import tsx`. `annotate-scene.ts`/`fact-check.ts` are stubs — generation goes through the Workflow templates. Adding a play needs **zero `site/` code changes**.

### Add a reusable reference card (standalone)
1. Decide kind: biblical, classical, or historical.
2. Append to `data/references/<kind>.json` (validates against `schemas/reference-card.schema.json`); preserve existing cards. The card's `kind` must match the file.
3. From an annotation, link via `references: [{ kind: "biblical", card_id: "cain-and-abel" }]`.

### Render the site locally
```
cd site
npm install
npm run dev
```
Visit `http://localhost:4321`. (When driving via the preview tools, `.claude/launch.json` defines a `site` config — `preview_start({ name: "site" })`.) On Windows, after adding a play, kill stray node processes so Astro re-scans `import.meta.glob`.

## Hard Rules

1. **Never edit anything inside [shakespeare-material-master/](shakespeare-material-master/).** It is vendored, read-only, and may be re-pulled.
2. **Never bypass schema validation in commits.** If a schema rejects valid data, the schema is wrong — fix it explicitly, don't `--no-verify`.
3. **Never publish an annotation without at least one source citation.** Unsourced glosses get `confidence: "uncertain"` and are excluded from the default depth.
4. **Every fact-check session must record verifier model IDs + the three verdicts in the annotation object.** Auditing requires this.
5. **`data/` is the source of truth.** The site reads from `data/`; it never writes back. If you want to change content, change the JSON.
6. **All scholarly claims must be falsifiable.** A biblical reference cites book:chapter:verse (Geneva Bible 1599). A classical reference cites the source work and book/line. A historical claim cites a date and a primary or canonical secondary source.
7. **When uncertain, route to the review queue.** `data/plays/<slug>/_review_queue/` (gitignored) is where the fact-checker drops candidates that need human eyes. Don't merge guesses into `annotations.json`.

## Out of Scope (v1)

- User accounts, login, persistent reader state
- Social or shared annotation
- Audio narration, video performance footage
- Quizzes, assessments, grade-tracking
- Translation to non-English languages
- Mobile apps (we are mobile-responsive but not native)

These may come later. Don't build for them now.

## Project Status

- **Plays shipping** (14):
  - **Hamlet** — 5 acts, 20 scenes, 3897 TLN'd lines; **356 annotations** (324 original + 32 rescued from the review queue), **24 reference cards** (30 links), `_review_queue/` cleared, **813-entry glossary**.
  - **A Midsummer Night's Dream** — 5 acts, 10 scenes, 2106 TLN'd lines; **189 annotations** (183 + 6 rescued from the review queue), wired to **7 reference cards** via 16 links, `_review_queue/` cleared, **724-entry glossary**.
  - **Julius Caesar** — 5 acts, 18 scenes, 2585 TLN'd lines; **202 annotations** (168 + 34 rescued from the review queue), wired to **18 reference cards** (14 new Roman/classical/historical + reused `alexander-and-caesar`, `fortunes-wheel`, `the-antique-roman`, `sixth-commandment`), `_review_queue/` cleared, **1030-entry glossary**. Heavy on `historical_topical` (42) and `classical_allusion` (29) — Plutarch's *Lives* (North 1579) is the play's source. Required a parse-gutenberg fix (unclosed `[` at source line 1113 was swallowing the rest of the file).
  - **Romeo and Juliet** — 5 acts, 24 scenes, 3059 TLN'd lines; **287 annotations** (262 + 25 rescued from the review queue), wired to **13 reference cards** via 34 links — 11 new (classical: `phaethon`, `diana-goddess`, `echo-nymph`, `helen-of-troy`; historical: `petrarch-and-the-sonnet`, `prince-of-cats`, `queen-mab`, `shrift-and-confession`, `palmers-and-pilgrims`, `elizabethan-plague`, `mandrake-folklore`) + reused `cupid-arrows`, `pyramus-thisbe`, `fortunes-wheel`, `sixth-commandment` — `_review_queue/` cleared, **1004-entry glossary**. Rich in `archaic_vocab` (104), `rhetorical_device` (36), `classical_allusion` (14), `bawdy_pun` (13). Required a **gated `parse-gutenberg.ts` enhancement**: the Act 1 Chorus prologue ('star-cross'd lovers') sits *before* `ACT I.` and the Act 2 prologue is a title-case `Chorus.`; both are now buffered (triggered by `THE PROLOGUE`/`Enter Chorus`) and prepended as TLN-numbered Chorus speech to the head of the act they introduce (Act 1 prologue = TLN 1-14). Two wrapped scene settings (3.5, 5.3) are rejoined. Hamlet/Midsummer/JC verified unaffected by static analysis (the change is gated behind a `chorus` speaker / "THE PROLOGUE" header / dangling-word setting, none of which they contain).
  - **Richard II** — 5 acts, 19 scenes, 2801 TLN'd lines; **262 annotations** (238 at build + 24 rescued from the review queue; 7 dropped of 269 candidates), wired to **12 reference cards** via 83 links, `_review_queue/` cleared (24 rescued), **891-entry glossary**. First history play; all-verse (clean ingest, no parser change). Heavy on `archaic_vocab` (76), `historical_topical` (67), `rhetorical_device` (42), `biblical_allusion` (21). Authored the **history "spine" cards** here: `edward-iii-and-his-sons`, `the-kings-two-bodies`, `the-great-chain-of-being`, `de-casibus-tragedy`, `trial-by-combat`, `holinshed-chronicles` (historical) + `pilate-washing-hands` (biblical). Speaker quirk: the bare `DUCHESS.` prefix covers two roles — Gloucester (1.2) and York (5.2/5.3) — deterministically reassigned post-ingest.
  - **Henry IV, Part 1** — 5 acts, 19 scenes, 2824 TLN'd lines; **369 annotations** (335 at build + 34 rescued from the review queue; 14 dropped of 383 candidates), wired to **16 reference cards** via 44 links — 2 new (`gads-hill`, `the-battle-of-shrewsbury`) + 2 new biblical (`the-prodigal-son`, `dives-and-lazarus`) + heavy **reuse of the R2 spine** (`holinshed-chronicles` 9, `edward-iii-and-his-sons` 6, `trial-by-combat`, `divine-right-of-kings`, `the-great-chain-of-being`, `garden-of-eden`) and earlier cards (`fortunes-wheel`, `diana-goddess`, `hyperion`, `hercules`, `phaethon`, `the-vice`) — `_review_queue/` cleared (34 rescued), **1044-entry glossary**. Introduces sustained **prose** (renders fine, no change needed). Richest in `archaic_vocab` (157), `historical_topical` (69), `rhetorical_device` (28), `wordplay` (25), `cultural_context` (24), `biblical_allusion` (15), `bawdy_pun` (4). Speaker quirk: this edition uses abbreviated prefixes (`POINTZ.`, `HOT.`, `FAL.`, `1. CAR.`, `SIR M.`, `LAN.`, `LADY.` = Lady Percy) — `characters.json` aliases must list the abbreviations exactly; one stray `GLEND.` label leaked into the Welsh-interpretation passage of 3.1 and was cleaned post-ingest.
  - **Henry IV, Part 2** — 5 acts, 19 scenes, 3010 TLN'd lines; **291 annotations** (270 at build + 21 rescued from the review queue; 8 dropped of 299 candidates), wired to **22 reference cards** via 54 links — 4 new (historical `oldcastle-and-falstaff`, `jerusalem-chamber`, `gascoigne-and-the-chief-justice`; classical `fama-rumour-personified`) + heavy reuse of the spine (`holinshed-chronicles` 14, `the-kings-two-bodies` 5, `edward-iii-and-his-sons` 4, plus `the-prodigal-son`/`dives-and-lazarus` authored last session *for* this play) — `_review_queue/` cleared (21 rescued), **1173-entry glossary**. Heavy on `archaic_vocab` (93), `historical_topical` (54), `rhetorical_device` (40), `cultural_context` (26). **Rumour's Induction** (headed `INDUCTION`, before `ACT I.`) and the closing **`EPILOGUE.`** (a Dancer) both needed the gated parser spike. Speaker quirk: `BARDOLPH.` (Falstaff's follower) vs `LORD BARDOLPH.` (a rebel lord) are two distinct roles — never conflate; `extract-speakers` false-positives (`Gray's Inn.`, `Samingo.`, `Norfolk.`) correctly stay as dialogue.
  - **Henry V** — 5 acts, 23 scenes, 3083 TLN'd lines; **302 annotations** (272 at build + 30 rescued from the review queue; 8 dropped of 310 candidates), wired to **23 reference cards** via 67 links — 6 new (historical `salic-law`, `the-battle-of-agincourt`, `saint-crispins-day`, `the-southampton-plot`, `the-wooden-o`, `crecy-and-poitiers`) + heavy reuse (`the-battle-of-agincourt` 11, `edward-iii-and-his-sons` 8, `holinshed-chronicles` 7, `salic-law` 6, `crecy-and-poitiers` 5, `alexander-and-caesar` 3, `garden-of-eden` 2 for Scroop's 'second fall of man', and opportunistic Roman cards `the-roman-triumph`/`pompey-the-great` for the Chorus's Caesar imagery) — `_review_queue/` cleared (30 rescued), **1125-entry glossary** (0 French). Heavy on `archaic_vocab` (107), `historical_topical` (64), `cultural_context` (28), `rhetorical_device` (27); **0 uncertain**. The flagship: needed the largest parser spike — **a Chorus before every act (bare `PROLOGUE.` header), ordinal-word act headers (`ACT FIRST.` mixed with roman `ACT II.`), and the closing `EPILOGUE.` sonnet**. **French scenes** (3.4, 5.2, Pistol's 4.4) carry plain-English translation glosses (basic `cultural_context`, 24 of them) + scholar bawdy notes, with French excluded from the glossary — per the locked decision (memory `french-passages-annotation-policy`). Speaker quirk: `extract-speakers` shares the *old* act-header blindness, so it skipped all of Act 1 and missed `CANTERBURY.`/`ELY.`/`AMBASSADOR.` (223+ lines) — the post-ingest unmapped-speaker check caught them and `characters.json` was backfilled.
  - **Henry VI, Part 1** — 5 acts, 27 scenes, 2713 TLN'd lines; **262 annotations** (242 at build + 20 rescued from the review queue; 8 dropped of 270 candidates), wired to **18 reference cards** via 109 links — 6 new (historical `wars-of-the-roses`, `joan-la-pucelle`, `lord-talbot`, `the-siege-of-orleans`, `the-order-of-the-garter`; classical `astraea`) + heavy reuse (`wars-of-the-roses` 19, `lord-talbot` 17, `holinshed-chronicles` 15, `joan-la-pucelle` 14, `edward-iii-and-his-sons` 9, `the-order-of-the-garter` 8, `the-siege-of-orleans` 8, `de-casibus-tragedy` 6) — `_review_queue/` cleared (20 rescued), **1093-entry glossary**. Heavy on `historical_topical` (96), `archaic_vocab` (79), `rhetorical_device` (30), `classical_allusion` (17). First play needing the **gated Henry VI parser normalizer** (see Known limitations). Speaker quirks: `PLANTAGENET.`=`YORK.` (created Duke of York in 3.1), `WINCHESTER.`=`CARDINAL.` (made Cardinal in Act 5), `FIRST/SECOND/THIRD SERVING-MAN.` (hyphens defeat `extract-speakers`); the cowardly **Sir John Fastolfe** is NOT Falstaff.
  - **Henry VI, Part 2** — 5 acts, 24 scenes, 3099 TLN'd lines; **307 annotations** (282 at build + 25 rescued from the review queue; 8 dropped of 315 candidates), wired to **15 reference cards** via 112 links — 4 new (historical `jack-cades-rebellion`, `eleanor-cobham-and-witchcraft`, `humphrey-duke-of-gloucester`, `the-first-battle-of-st-albans`) + heavy reuse (`jack-cades-rebellion` 21, `humphrey-duke-of-gloucester` 20, `eleanor-cobham-and-witchcraft` 16, `wars-of-the-roses` 12, `edward-iii-and-his-sons` 12, `de-casibus-tragedy` 8, `trial-by-combat` 3 for the armorer's duel) — `_review_queue/` cleared (25 rescued), **1051-entry glossary**. Heavy on `archaic_vocab` (89), `historical_topical` (82), `rhetorical_device` (43), `cultural_context` (23). Speaker quirk: `BOLINGBROKE.` is **Roger Bolingbroke the conjurer** (1.4), NOT the usurping king of Richard II; the witch `M. JOURDAIN.` (Margery Jourdain) + `SPIRIT.` (Asnath); `KING.`=`KING HENRY.`, `QUEEN.`=Margaret, `CLIFFORD.`/`LORD CLIFFORD.` one role vs `YOUNG CLIFFORD.` his son.
  - **Henry VI, Part 3** — 5 acts, 28 scenes, 2936 TLN'd lines; **261 annotations** (261 at build; 20 to `_review_queue/`, 9 dropped, 1 anchor-unresolved of 290 candidates), wired to **25 reference cards** via 134 links — 2 new (`the-battle-of-towton`, `the-stage-machiavel`) + 1 classical (`daedalus-and-icarus`) + heavy reuse (`wars-of-the-roses` 40, `holinshed-chronicles` 27, `edward-iii-and-his-sons` 7, `the-first-battle-of-st-albans` 7, `de-casibus-tragedy` 7, `fortunes-wheel` 5) — `_review_queue/` 20 (deferred), **1054-entry glossary**. Heavy on `historical_topical` (84), `archaic_vocab` (72), `rhetorical_device` (45), `classical_allusion` (31). Same Henry VI edition as Parts 1-2 (slug-gated `normalizeHenryVIEdition`), though here settings are inline and SDs bracketed, so the normalizer is a near-no-op. Speaker conflations: `RICHARD.`/`GLOSTER.`/`Gloster.` = the future Richard III (NOT Humphrey of Part 2); `EDWARD.`/`KING EDWARD.` = Edward IV; `PRINCE.` = the Lancastrian Prince Edward (a *distinct* Edward); `GEORGE.`/`CLARENCE.` one role; `LADY GREY.`/`QUEEN ELIZABETH.` one role; the three `1/2/3 WATCHMAN.` need explicit aliases. Surfaced and required the **gated inline-SD parser fix** (Known limitations #7) for Richard's `[Aside.] Not I.` in 4.1.
  - **Richard III** — 5 acts, 25 scenes, 3657 TLN'd lines; **342 annotations** (342 at build; 22 to `_review_queue/`, 9 dropped, 6 anchor-unresolved of 373 candidates), wired to **20 reference cards** via 184 links — the Richard III delta (`mores-history-of-richard-iii` 48, `princes-in-the-tower` 17, `the-stage-machiavel` 15, `richmond-and-the-tudor-myth` 13, `the-battle-of-bosworth-field` 9, `the-white-boar` 8) atop heavy reuse (`wars-of-the-roses` 22, `the-vice` 12, `holinshed-chronicles` 11, `de-casibus-tragedy`, `divine-right-of-kings`, `fortunes-wheel`, `pilate-washing-hands`, `cain-and-abel`, `sixth-commandment`) — `_review_queue/` 22 (deferred), **1000-entry glossary**. The much-taught payoff play (completes the first tetralogy): heavy on `historical_topical` (125), `archaic_vocab` (86), `rhetorical_device` (71). Standard edition (NOT the Henry VI normalizer). Speaker conflations: `GLOSTER.`/`KING RICHARD.` one role (the title villain — KING RICHARD after his Act 4 coronation); `YORK.` = the **young prince** Duke of York (NOT the long-dead Duke); `QUEEN MARGARET.` = the aged cursing widow; `QUEEN ELIZABETH.` = Edward IV's widow (the Lady Grey of H6.3). The opening 'Now is the winter' label is a bare `GLOSTER` with **no period** — needed a no-dot alias so the soliloquy attributes to Richard, not a phantom speaker.
  - **King John** — 5 acts, 16 scenes, 2649 TLN'd lines; **254 annotations** (234 at build + 20 rescued from the review queue; 5 dropped, 0 anchor-unresolved of 259 candidates), wired to **17 reference cards** via 79 links — 4 new (historical `the-angevin-succession` [Arthur's claim vs John's title by 'strong possession', cited 15×], `the-papal-interdict` [Pandulph, excommunication, John's submission to Rome, 17×], `richard-the-lionheart` [Coeur-de-lion, the lion-skin, Austria, 8×], `the-troublesome-raigne` [the anon. 1591 source play, 6×]) + reuse (`holinshed-chronicles` 11, `fortunes-wheel` 4, `nemean-lion` 3, `divine-right-of-kings` 3, `de-casibus-tragedy` 3, `the-kings-two-bodies` 2, plus `cain-and-abel`, `hercules`, `ate`, `nero`, `garden-of-eden`, `the-great-chain-of-being`) — `_review_queue/` cleared (20 rescued), **1027-entry glossary**. First of the two outlier histories; clean verse/prose ingest (12th–13th c. Angevin setting, distinct from the later Edward III genealogy; the play notably OMITS Magna Carta). Heavy on `archaic_vocab` (73), `historical_topical` (64), `rhetorical_device` (62, the formal lamentation/stichomythia), `cultural_context` (21). Parser/speaker quirks: the no-dot `CONSTANCE` prefix (source line 1622) needed a no-dot alias, else her 3.1 speech merged into Elinor's; the unison prefix `PEMBROKE. and BIGOT.` (20 chars) exceeded `looksLikeSpeaker`'s 18-char cap — fixed by a self-gating unison rule (≥2 dotted ALL-CAPS known aliases accepted regardless of length). The Bastard = Philip Faulconbridge (Richard I's bastard son), the play's choric conscience; `KING PHILIP.` is the French king (not the Bastard).
  - **Henry VIII** — 5 acts, 17 scenes, 3240 TLN'd lines; **303 annotations** (282 at build + 21 rescued from the review queue; 16 dropped, 5 anchor-unresolved of 319 candidates), wired to **15 reference cards** via 159 links — 6 new historical (`cardinal-wolsey` [the butcher's son's rise and fall, cited 43×], `the-kings-great-matter` [the divorce/Blackfriars trial, 38×], `the-break-with-rome` [Reformation/royal supremacy, 19×], `foxes-acts-and-monuments` [Foxe's Book of Martyrs, the Cranmer/Act-5 source, 13×], `cranmers-prophecy-of-elizabeth` [the 5.5 panegyric, 8×], `field-of-the-cloth-of-gold` [the 1520 summit, 1.1, 4×]) + 2 new biblical (`the-fall-of-the-angels` [Lucifer's pride — Wolsey's 'by that sin fell the angels', 3×], `queen-of-sheba` ['Saba', 5.5, 1×]) + reuse (`de-casibus-tragedy` 17 — the three falls of the mighty, plus `divine-right-of-kings`, `holinshed-chronicles`, `fortunes-wheel`, `cain-and-abel`, `the-kings-two-bodies`, `the-great-chain-of-being`) — `_review_queue/` cleared (21 rescued), **1000-entry glossary**. The late, **Fletcher-co-authored** outlier (the church-state pair's Reformation play); pageant-dense (4 long procession/vision/coronation/christening stage directions render fine). Heavy on `historical_topical` (121), `archaic_vocab` (80), `rhetorical_device` (52), `cultural_context` (22). Parser: needed a **gated speaker-less-prologue fix** (`THE PROLOGUE.` is followed straight by verse with no Chorus label — now attributed to a `Prologue` chorus speaker, gated on a `PROLOGUE.` alias / `hasPrologueSpeaker`; the bare `EPILOGUE` reads as an `Epilogue` chorus via the existing SONG_HEADER-alias mechanism) and a **gated inline-speaker split** (`NORFOLK. I thank your Grace,` opens a mid-verse-line, slug-gated `INLINE_SPEAKER_GUARD`). Speaker quirks: inconsistent periods (no-dot `BUCKINGHAM` ×7 in 1.1), `QUEEN KATHERINE.`/`KATHERINE.` one role, `BOTH.`/`ALL.` unison ensembles.
  - 29 other works are catalog-only.
- **Reference cards: 106 total** (94 prior + 12 new in Session 5 — historical: `the-angevin-succession`, `the-papal-interdict`, `richard-the-lionheart`, `the-troublesome-raigne` (King John); `cardinal-wolsey` (cited 43×), `the-kings-great-matter` (38×), `the-break-with-rome`, `foxes-acts-and-monuments`, `cranmers-prophecy-of-elizabeth`, `field-of-the-cloth-of-gold` (Henry VIII); biblical: `the-fall-of-the-angels` (Lucifer's pride — Wolsey's 'by that sin fell the angels'; reusable corpus-wide for ambition, e.g. Macbeth), `queen-of-sheba` (the 'Saba' of Cranmer's prophecy)). With King John + Henry VIII shipped, **all ten English history plays now ship and are card-complete**, and all 12 new cards were used (the targeted-delta reuse thesis held a 5th time). With Richard III the **first tetralogy is card-complete**: `wars-of-the-roses` (the cross-tetralogy keystone) was reused 40 (H6.3) + 22 (R3); `edward-iii-and-his-sons` is the genealogical keystone cited by every history. The history "spine" (`edward-iii-and-his-sons`, `the-kings-two-bodies`, `the-great-chain-of-being`, `de-casibus-tragedy`, `trial-by-combat`, `holinshed-chronicles`, `pilate-washing-hands`) plus `divine-right-of-kings`/`the-vice`/`fortunes-wheel`/`cain-and-abel` remain corpus-wide infrastructure; `holinshed-chronicles` carries `related_plays` for all 10 histories + Macbeth. `the-stage-machiavel` and `daedalus-and-icarus` are cross-corpus conceptual cards (reusable for Iago/Edmund/Aaron, and for the Icarus-image of ambition).
- **Live on GitHub Pages**: <https://jd-jones-ases.github.io/shakespeare-portal/> — public repo `JD-Jones-ASES/shakespeare-portal`, deployed by `.github/workflows/deploy.yml` (CI builds the search index, then `astro build`, then `deploy-pages`). Astro `base: '/shakespeare-portal/'`; every internal link routes through `site/src/utils/url.ts` `withBase()` (add it to any new link or `public/` fetch). The landing page (`pages/index.astro`) is a status-aware launch: hero + a 14-of-43 progress bar, a build-time **stats band**, a **"Now open"** shelf of the 14 live plays as rich cards, and a genre-grouped **"In the wings"** catalog of the rest. (The progress bar / stats band are build-time computed from `data/`, so they update automatically.)
- **2026-05-31 accuracy sweep**: verified all **55 reference cards** (tightened 3 citations — `hyperion` Ovid locus, `mandrake-folklore` Pliny book, `prince-of-cats` Nashe quote, the last confirmed *correct* against the 1596 text); audited every `medium`/`uncertain` annotation (fixed 6, incl. Cicero *De Amicitia* §80 and the Geneva 1 Peter 3:7 "woman"); and **cleared all four `_review_queue/`s — 97 items rescued with corrected or generalized (verifiable) citations, 0 cut**. The failures were citation-precision issues, not bad glosses, so the fix was to make citations honest (drop fabricated/wrong loci, generalize to work level, fix Geneva-vs-KJV wording) and trim 4 interpretation-overreaches to their sourced cores. Total annotations **937 → 1034**; all three validators green.
- **Reader features (all live & verified in-browser)**:
  - **Warm-literary design system** with light/dark/auto **theme toggle** (`components/ThemeToggle.tsx`, no-flash inline script in `Base.astro`, tokens under `:root[data-theme]`); serif play text (EB Garamond).
  - **Read / Study depth tiers** (`DepthToggle.tsx`): Read = clean plain-English glosses only; Study = full apparatus (detail, source citations, full reference cards, plus `bawdy_pun`/`textual_variant`/scholar notes). Markers for Study-only notes are hidden in Read.
  - **Character reading system** (`components/ReadingControls.tsx`): per-character colors (`color` field in `characters.json`); modes Normal / Color-speakers / Highlight / **Focus** (with Prev/Next-line cue jumps for reading a part aloud) for classroom reading. Speaker name printed **once per speech** with a colored stripe (Folger style).
  - **Instant hover/tap word glossary** (`components/SceneReader.astro` + `site/src/data/glossary.ts`): archaic words get a subtle underline + tooltip (`.gloss-term` + `data-def`); "word help" on/off toggle.
  - Two-way gloss↔line linking (`ReaderInteractions.astro`); marginal gloss sidebar with reference cards; **full-play search** (header `SearchBar.tsx` + `build-index.ts`); per-act/scene **synopsis** on the landing page. Manual test script: [docs/PROTOTYPE_TEST.md](docs/PROTOTYPE_TEST.md).
- **Pipeline proven on fourteen plays** (four pilots + all ten English histories — the Henriad *Richard II–Henry V*, the complete first tetralogy *Henry VI Parts 1-3 + Richard III*, and the two outliers *King John + Henry VIII*): ingest → Sonnet annotator subagents → deterministic anchor resolution (`resolve-anchors.ts`, shared matcher in `lib/text.ts`) → adversarial fact-check (source + interpretation Sonnet judges + deterministic anchor judge, merged by `apply-verdicts.ts`) → validators → Astro reader. Glossary uses the same generate→fact-check pattern (`merge-glossary.ts`, `apply-glossary-verdicts.ts`). **`reanchor.ts`** re-resolves shipped annotations after a re-ingest so TLN shifts don't break anchors. All 3 validators pass; the site builds. Julius Caesar was run via the **Workflow** tool (one annotator subagent per scene → source+interp judges per scene), then the deterministic merge steps run serially from inside `scripts/` (must use `node --import tsx …` from `scripts/`; `node`'s `execFileSync` can't spawn the `.bin/tsx.cmd` shim on Windows — EINVAL). Romeo and Juliet ran the same way (24 annotators + 48 judges, then a 5-act glossary fan-out). The `workflows/annotate.mjs` and `workflows/glossary.mjs` templates were hardened to accept `args` delivered as a JSON **string** as well as an object (`const input = typeof args === 'string' ? JSON.parse(args) : args`) — without it the Workflow tool can throw "Workflow args must be…" before any agent runs. **Session 2** (H4.2 + Henry V) added an optional `guidance` arg to both Workflow templates (appended to every annotator/lexicographer prompt) — used for Henry V's French-translation instructions and the French-glossary exclusion; it is the reusable hook for H6.1's Joan. **Concurrency lesson:** running the two plays' heavy annotate Workflows *concurrently* (~126 agents) saturated the machine and ~55-60% of later-scene annotator agents finished WITHOUT writing their candidate files (the workflow still reports all agents "completed"); the candidate files were simply missing (not malformed). Re-running the missing scenes **one workflow at a time** fixed it cleanly. **Rule: at most one heavy annotate Workflow at a time** (a light ~5-agent glossary Workflow alongside is fine). The `_check-candidates` / unmapped-speaker / detail-summary-fix helpers were throwaway diagnostics (recipe in memory `henriad-b-build-notes`). **Session 4** (Henry VI Part 3 + Richard III) re-confirmed the concurrency rule — one heavy annotate + one light glossary per play, sequential across plays — with **0 candidate loss (290/290 and 373/373)**. **Session 5** (King John + Henry VIII) confirmed it a 4th time (259/259 and 319/319, 0 loss) and cleared **all four outstanding `_review_queue/`s in a single 4-agent Opus rescue Workflow — 83 items rescued, 0 cut** (John 20 + Henry VIII 21 + the deferred Henry VI Part 3 20 + Richard III 22) via `scripts/pipeline/_merge-rescued.mjs`; one rescued classical citation (the Olympian games, correctly re-sourced to Pindar/Pausanias) prompted adding those two ancient authors to the citation allowlist.
- **Next**: **The 5-session history-plays plan is COMPLETE** (Session 5 = King John + Henry VIII shipped 2026-06-01) — **all ten English history plays now ship**. The portal ships **14 plays / 4,029 annotations / 106 reference cards**, and **all `_review_queue/`s are empty corpus-wide** (every deferred queue rescued). The 29 remaining catalog-only works are open for the same runbook: the **Holinshed-sourced tragedies (Macbeth, King Lear, Cymbeline) are the natural next block** — they inherit the history "spine" cards directly (`divine-right-of-kings`, `the-kings-two-bodies`, `de-casibus-tragedy`, `the-great-chain-of-being`, `fortunes-wheel`, `cain-and-abel`). Other clusters: the Roman tragedies (Antony and Cleopatra, Coriolanus, Titus — reuse the JC Roman cards), the romances (The Tempest, Winter's Tale, Pericles), and the comedies. Deferred infrastructure (unchanged): accessibility toolbar, modern-English paraphrase, wiring `@anthropic-ai/sdk` into the stubbed `annotate-scene.ts`/`fact-check.ts` for headless/CI.
- **Known limitations**: (1) anchor highlighting is word-granular, so at a source's glued `;--`/`,--` token a highlight may over-cover by the adjacent word; (2) annotator/glossary subagents occasionally emit JSON with unescaped inner double-quotes in citations — prompts include "use single quotes inside strings"; (3) `_review_queue/`, `_candidates/`, and `_*` files are gitignored — they exist on disk but not in version control; (4) on Windows, Astro's dev-server caches `import.meta.glob`; after adding a play's `text.json`, kill stray `node` processes before restarting; (5) some Gutenberg editions have an unclosed `[` stage-direction bracket that, before the guard added to `parse-gutenberg.ts`, made the bracket-chaser swallow the rest of the file — the parser now stops chasing at a blank line or ACT/SCENE header; (6) the Open Shakespeare **Henry VI** transcription uses a different convention — ordinal/no-period act headers (`ACT FIRST`), scene settings on the line *after* the SCENE header (so `Westminster Abbey.` was misread as a speaker), and many *unbracketed* stage directions (`Dead March. Enter the funeral...`). The **slug-gated** `normalizeHenryVIEdition` in `parse-gutenberg.ts` folds separate-line settings into the SCENE header and wraps an unbracketed-SD block ONLY when its first line is a high-confidence stage cue (Enter/Exeunt/Alarum/Flourish/Dead March/etc.), so a dialogue continuation after an in-speech blank line is never swallowed; the 8 prior plays re-ingest byte-identical. Added `Herodotus` to the citation allowlist (Charles's Rhodope, 1H6 1.6). (7) an **inline stage direction at the START of a dialogue line** (`[Aside.] Not I.`, 3H6 4.1; cf. Hamlet's Ghost `[Beneath.] Swear.`): the short reply looks like a speech prefix, and because `emitSD` sets `expectSpeaker`, the trailing words were misread as a phantom speaker and the line lost. Fixed in `parse-gutenberg.ts` (`dispatchText` no longer treats text *after* an inline SD as a possible speaker), **gated on the Henry VI slugs** (`INLINE_SD_GUARD`) so the 10 already-shipped plays re-ingest byte-identical — verified (Hamlet's 4 Ghost 'Swear.' cries are intentionally left as-is; correcting them would shift Hamlet's TLNs and break its shipped annotation anchors). Richard III ingests clean without the guard, so it stays off there. (8) **Henry VIII / King John quirks (Session 5, all gated so the 12 prior plays re-ingest byte-identical).** Henry VIII's `THE PROLOGUE.` is followed directly by verse with NO Chorus speaker label (unlike Romeo and Juliet / Henry V), so the buffered prologue had no speaker and would be dropped; fixed by attributing it to a `Prologue` chorus when the play defines a `PROLOGUE.` alias (`hasPrologueSpeaker`), while the closing bare `EPILOGUE` reads as an `Epilogue` chorus through the existing SONG_HEADER-alias path. Henry VIII also occasionally prints an inline speaker label opening a shared verse-line (`NORFOLK. I thank your Grace,`, 1.1) — split off as a new speaker, gated `INLINE_SPEAKER_GUARD = slug === 'henry_viii'`. King John needed a no-dot `CONSTANCE` alias (the source drops the period at line 1622, else her 3.1 speech merges into Elinor's) and a `looksLikeSpeaker` tweak so a unison prefix (≥2 dotted ALL-CAPS known aliases, e.g. `PEMBROKE. and BIGOT.`) is accepted regardless of length — JC's 18-char cap had rejected the 20-char unison (self-gating: requires this play's aliases). Added `Pindar` and `Pausanias` to the classical citation allowlist (the Olympian games, 3 Henry VI 2.3, re-sourced during the rescue sweep). Regression-verified: re-ingesting all 12 prior plays changes only the date stamp (save the documented R2-DUCHESS / H4.1-GLEND post-ingest cleanups, which revert on a naive re-ingest and are restored with `git checkout`).
