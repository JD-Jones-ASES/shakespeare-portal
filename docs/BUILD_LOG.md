# Shakespeare Portal — Build Log

The per-play changelog and the session-by-session pipeline/parser history. `CLAUDE.md`'s **Project
Status** stays high-level (counts, what's live, process rules, next); the detail behind each shipped
play lives here. Consult this when you need the specifics behind a play, a reference-card decision, or
a gated parser normalizer. Append newest work at the end of each section.

---

## Shipped plays (per-play detail)

- **Hamlet** — 5 acts, 20 scenes, 3897 TLN'd lines; **356 annotations** (324 original + 32 rescued from the review queue), **24 reference cards** (30 links), `_review_queue/` cleared, **813-entry glossary**.
- **A Midsummer Night's Dream** — 5 acts, 10 scenes, 2106 TLN'd lines; **189 annotations** (183 + 6 rescued from the review queue), wired to **7 reference cards** via 16 links, `_review_queue/` cleared, **724-entry glossary**.
- **Julius Caesar** — 5 acts, 18 scenes, 2585 TLN'd lines; **202 annotations** (168 + 34 rescued from the review queue), wired to **18 reference cards** (14 new Roman/classical/historical + reused `alexander-and-caesar`, `fortunes-wheel`, `the-antique-roman`, `sixth-commandment`), `_review_queue/` cleared, **1030-entry glossary**. Heavy on `historical_topical` (42) and `classical_allusion` (29) — Plutarch's *Lives* (North 1579) is the play's source. Required a parse-gutenberg fix (unclosed `[` at source line 1113 was swallowing the rest of the file).
- **Romeo and Juliet** — 5 acts, 24 scenes, 3059 TLN'd lines; **287 annotations** (262 + 25 rescued from the review queue), wired to **13 reference cards** via 34 links — 11 new (classical: `phaethon`, `diana-goddess`, `echo-nymph`, `helen-of-troy`; historical: `petrarch-and-the-sonnet`, `prince-of-cats`, `queen-mab`, `shrift-and-confession`, `palmers-and-pilgrims`, `elizabethan-plague`, `mandrake-folklore`) + reused `cupid-arrows`, `pyramus-thisbe`, `fortunes-wheel`, `sixth-commandment` — `_review_queue/` cleared, **1004-entry glossary**. Rich in `archaic_vocab` (104), `rhetorical_device` (36), `classical_allusion` (14), `bawdy_pun` (13). Required a **gated `parse-gutenberg.ts` enhancement**: the Act 1 Chorus prologue ('star-cross'd lovers') sits *before* `ACT I.` and the Act 2 prologue is a title-case `Chorus.`; both are now buffered (triggered by `THE PROLOGUE`/`Enter Chorus`) and prepended as TLN-numbered Chorus speech to the head of the act they introduce (Act 1 prologue = TLN 1-14). Two wrapped scene settings (3.5, 5.3) are rejoined. Hamlet/Midsummer/JC verified unaffected by static analysis (the change is gated behind a `chorus` speaker / "THE PROLOGUE" header / dangling-word setting, none of which they contain).
- **Richard II** — 5 acts, 19 scenes, 2801 TLN'd lines; **262 annotations** (238 at build + 24 rescued from the review queue; 7 dropped of 269 candidates), wired to **12 reference cards** via 83 links, `_review_queue/` cleared (24 rescued), **891-entry glossary**. First history play; all-verse (clean ingest, no parser change). Heavy on `archaic_vocab` (76), `historical_topical` (67), `rhetorical_device` (42), `biblical_allusion` (21). Authored the **history "spine" cards** here: `edward-iii-and-his-sons`, `the-kings-two-bodies`, `the-great-chain-of-being`, `de-casibus-tragedy`, `trial-by-combat`, `holinshed-chronicles` (historical) + `pilate-washing-hands` (biblical). Speaker quirk: the bare `DUCHESS.` prefix covers two roles — Gloucester (1.2) and York (5.2/5.3) — deterministically reassigned post-ingest.
- **Henry IV, Part 1** — 5 acts, 19 scenes, 2824 TLN'd lines; **369 annotations** (335 at build + 34 rescued from the review queue; 14 dropped of 383 candidates), wired to **16 reference cards** via 44 links — 2 new (`gads-hill`, `the-battle-of-shrewsbury`) + 2 new biblical (`the-prodigal-son`, `dives-and-lazarus`) + heavy **reuse of the R2 spine** (`holinshed-chronicles` 9, `edward-iii-and-his-sons` 6, `trial-by-combat`, `divine-right-of-kings`, `the-great-chain-of-being`, `garden-of-eden`) and earlier cards (`fortunes-wheel`, `diana-goddess`, `hyperion`, `hercules`, `phaethon`, `the-vice`) — `_review_queue/` cleared (34 rescued), **1044-entry glossary**. Introduces sustained **prose** (renders fine, no change needed). Richest in `archaic_vocab` (157), `historical_topical` (69), `rhetorical_device` (28), `wordplay` (25), `cultural_context` (24), `biblical_allusion` (15), `bawdy_pun` (4). Speaker quirk: this edition uses abbreviated prefixes (`POINTZ.`, `HOT.`, `FAL.`, `1. CAR.`, `SIR M.`, `LAN.`, `LADY.` = Lady Percy) — `characters.json` aliases must list the abbreviations exactly; one stray `GLEND.` label leaked into the Welsh-interpretation passage of 3.1 and was cleaned post-ingest.
- **Henry IV, Part 2** — 5 acts, 19 scenes, 3010 TLN'd lines; **291 annotations** (270 at build + 21 rescued from the review queue; 8 dropped of 299 candidates), wired to **22 reference cards** via 54 links — 4 new (historical `oldcastle-and-falstaff`, `jerusalem-chamber`, `gascoigne-and-the-chief-justice`; classical `fama-rumour-personified`) + heavy reuse of the spine (`holinshed-chronicles` 14, `the-kings-two-bodies` 5, `edward-iii-and-his-sons` 4, plus `the-prodigal-son`/`dives-and-lazarus` authored last session *for* this play) — `_review_queue/` cleared (21 rescued), **1173-entry glossary**. Heavy on `archaic_vocab` (93), `historical_topical` (54), `rhetorical_device` (40), `cultural_context` (26). **Rumour's Induction** (headed `INDUCTION`, before `ACT I.`) and the closing **`EPILOGUE.`** (a Dancer) both needed the gated parser spike. Speaker quirk: `BARDOLPH.` (Falstaff's follower) vs `LORD BARDOLPH.` (a rebel lord) are two distinct roles — never conflate; `extract-speakers` false-positives (`Gray's Inn.`, `Samingo.`, `Norfolk.`) correctly stay as dialogue.
- **Henry V** — 5 acts, 23 scenes, 3083 TLN'd lines; **302 annotations** (272 at build + 30 rescued from the review queue; 8 dropped of 310 candidates), wired to **23 reference cards** via 67 links — 6 new (historical `salic-law`, `the-battle-of-agincourt`, `saint-crispins-day`, `the-southampton-plot`, `the-wooden-o`, `crecy-and-poitiers`) + heavy reuse (`the-battle-of-agincourt` 11, `edward-iii-and-his-sons` 8, `holinshed-chronicles` 7, `salic-law` 6, `crecy-and-poitiers` 5, `alexander-and-caesar` 3, `garden-of-eden` 2 for Scroop's 'second fall of man', and opportunistic Roman cards `the-roman-triumph`/`pompey-the-great` for the Chorus's Caesar imagery) — `_review_queue/` cleared (30 rescued), **1125-entry glossary** (0 French). Heavy on `archaic_vocab` (107), `historical_topical` (64), `cultural_context` (28), `rhetorical_device` (27); **0 uncertain**. The flagship: needed the largest parser spike — **a Chorus before every act (bare `PROLOGUE.` header), ordinal-word act headers (`ACT FIRST.` mixed with roman `ACT II.`), and the closing `EPILOGUE.` sonnet**. **French scenes** (3.4, 5.2, Pistol's 4.4) carry plain-English translation glosses (basic `cultural_context`, 24 of them) + scholar bawdy notes, with French excluded from the glossary — per the locked decision (memory `french-passages-annotation-policy`). Speaker quirk: `extract-speakers` shares the *old* act-header blindness, so it skipped all of Act 1 and missed `CANTERBURY.`/`ELY.`/`AMBASSADOR.` (223+ lines) — the post-ingest unmapped-speaker check caught them and `characters.json` was backfilled.
- **Henry VI, Part 1** — 5 acts, 27 scenes, 2713 TLN'd lines; **262 annotations** (242 at build + 20 rescued from the review queue; 8 dropped of 270 candidates), wired to **18 reference cards** via 109 links — 6 new (historical `wars-of-the-roses`, `joan-la-pucelle`, `lord-talbot`, `the-siege-of-orleans`, `the-order-of-the-garter`; classical `astraea`) + heavy reuse (`wars-of-the-roses` 19, `lord-talbot` 17, `holinshed-chronicles` 15, `joan-la-pucelle` 14, `edward-iii-and-his-sons` 9, `the-order-of-the-garter` 8, `the-siege-of-orleans` 8, `de-casibus-tragedy` 6) — `_review_queue/` cleared (20 rescued), **1093-entry glossary**. Heavy on `historical_topical` (96), `archaic_vocab` (79), `rhetorical_device` (30), `classical_allusion` (17). First play needing the **gated Henry VI parser normalizer** (see Known limitations). Speaker quirks: `PLANTAGENET.`=`YORK.` (created Duke of York in 3.1), `WINCHESTER.`=`CARDINAL.` (made Cardinal in Act 5), `FIRST/SECOND/THIRD SERVING-MAN.` (hyphens defeat `extract-speakers`); the cowardly **Sir John Fastolfe** is NOT Falstaff.
- **Henry VI, Part 2** — 5 acts, 24 scenes, 3099 TLN'd lines; **307 annotations** (282 at build + 25 rescued from the review queue; 8 dropped of 315 candidates), wired to **15 reference cards** via 112 links — 4 new (historical `jack-cades-rebellion`, `eleanor-cobham-and-witchcraft`, `humphrey-duke-of-gloucester`, `the-first-battle-of-st-albans`) + heavy reuse (`jack-cades-rebellion` 21, `humphrey-duke-of-gloucester` 20, `eleanor-cobham-and-witchcraft` 16, `wars-of-the-roses` 12, `edward-iii-and-his-sons` 12, `de-casibus-tragedy` 8, `trial-by-combat` 3 for the armorer's duel) — `_review_queue/` cleared (25 rescued), **1051-entry glossary**. Heavy on `archaic_vocab` (89), `historical_topical` (82), `rhetorical_device` (43), `cultural_context` (23). Speaker quirk: `BOLINGBROKE.` is **Roger Bolingbroke the conjurer** (1.4), NOT the usurping king of Richard II; the witch `M. JOURDAIN.` (Margery Jourdain) + `SPIRIT.` (Asnath); `KING.`=`KING HENRY.`, `QUEEN.`=Margaret, `CLIFFORD.`/`LORD CLIFFORD.` one role vs `YOUNG CLIFFORD.` his son.
- **Henry VI, Part 3** — 5 acts, 28 scenes, 2936 TLN'd lines; **261 annotations** (261 at build; 20 to `_review_queue/`, 9 dropped, 1 anchor-unresolved of 290 candidates), wired to **25 reference cards** via 134 links — 2 new (`the-battle-of-towton`, `the-stage-machiavel`) + 1 classical (`daedalus-and-icarus`) + heavy reuse (`wars-of-the-roses` 40, `holinshed-chronicles` 27, `edward-iii-and-his-sons` 7, `the-first-battle-of-st-albans` 7, `de-casibus-tragedy` 7, `fortunes-wheel` 5) — `_review_queue/` 20 (deferred), **1054-entry glossary**. Heavy on `historical_topical` (84), `archaic_vocab` (72), `rhetorical_device` (45), `classical_allusion` (31). Same Henry VI edition as Parts 1-2 (slug-gated `normalizeHenryVIEdition`), though here settings are inline and SDs bracketed, so the normalizer is a near-no-op. Speaker conflations: `RICHARD.`/`GLOSTER.`/`Gloster.` = the future Richard III (NOT Humphrey of Part 2); `EDWARD.`/`KING EDWARD.` = Edward IV; `PRINCE.` = the Lancastrian Prince Edward (a *distinct* Edward); `GEORGE.`/`CLARENCE.` one role; `LADY GREY.`/`QUEEN ELIZABETH.` one role; the three `1/2/3 WATCHMAN.` need explicit aliases. Surfaced and required the **gated inline-SD parser fix** (Known limitations #7) for Richard's `[Aside.] Not I.` in 4.1.
- **Richard III** — 5 acts, 25 scenes, 3657 TLN'd lines; **342 annotations** (342 at build; 22 to `_review_queue/`, 9 dropped, 6 anchor-unresolved of 373 candidates), wired to **20 reference cards** via 184 links — the Richard III delta (`mores-history-of-richard-iii` 48, `princes-in-the-tower` 17, `the-stage-machiavel` 15, `richmond-and-the-tudor-myth` 13, `the-battle-of-bosworth-field` 9, `the-white-boar` 8) atop heavy reuse (`wars-of-the-roses` 22, `the-vice` 12, `holinshed-chronicles` 11, `de-casibus-tragedy`, `divine-right-of-kings`, `fortunes-wheel`, `pilate-washing-hands`, `cain-and-abel`, `sixth-commandment`) — `_review_queue/` 22 (deferred), **1000-entry glossary**. The much-taught payoff play (completes the first tetralogy): heavy on `historical_topical` (125), `archaic_vocab` (86), `rhetorical_device` (71). Standard edition (NOT the Henry VI normalizer). Speaker conflations: `GLOSTER.`/`KING RICHARD.` one role (the title villain — KING RICHARD after his Act 4 coronation); `YORK.` = the **young prince** Duke of York (NOT the long-dead Duke); `QUEEN MARGARET.` = the aged cursing widow; `QUEEN ELIZABETH.` = Edward IV's widow (the Lady Grey of H6.3). The opening 'Now is the winter' label is a bare `GLOSTER` with **no period** — needed a no-dot alias so the soliloquy attributes to Richard, not a phantom speaker.
- **King John** — 5 acts, 16 scenes, 2649 TLN'd lines; **254 annotations** (234 at build + 20 rescued from the review queue; 5 dropped, 0 anchor-unresolved of 259 candidates), wired to **17 reference cards** via 79 links — 4 new (historical `the-angevin-succession` [Arthur's claim vs John's title by 'strong possession', cited 15×], `the-papal-interdict` [Pandulph, excommunication, John's submission to Rome, 17×], `richard-the-lionheart` [Coeur-de-lion, the lion-skin, Austria, 8×], `the-troublesome-raigne` [the anon. 1591 source play, 6×]) + reuse (`holinshed-chronicles` 11, `fortunes-wheel` 4, `nemean-lion` 3, `divine-right-of-kings` 3, `de-casibus-tragedy` 3, `the-kings-two-bodies` 2, plus `cain-and-abel`, `hercules`, `ate`, `nero`, `garden-of-eden`, `the-great-chain-of-being`) — `_review_queue/` cleared (20 rescued), **1027-entry glossary**. First of the two outlier histories; clean verse/prose ingest (12th–13th c. Angevin setting, distinct from the later Edward III genealogy; the play notably OMITS Magna Carta). Heavy on `archaic_vocab` (73), `historical_topical` (64), `rhetorical_device` (62, the formal lamentation/stichomythia), `cultural_context` (21). Parser/speaker quirks: the no-dot `CONSTANCE` prefix (source line 1622) needed a no-dot alias, else her 3.1 speech merged into Elinor's; the unison prefix `PEMBROKE. and BIGOT.` (20 chars) exceeded `looksLikeSpeaker`'s 18-char cap — fixed by a self-gating unison rule (≥2 dotted ALL-CAPS known aliases accepted regardless of length). The Bastard = Philip Faulconbridge (Richard I's bastard son), the play's choric conscience; `KING PHILIP.` is the French king (not the Bastard).
- **Henry VIII** — 5 acts, 17 scenes, 3240 TLN'd lines; **303 annotations** (282 at build + 21 rescued from the review queue; 16 dropped, 5 anchor-unresolved of 319 candidates), wired to **15 reference cards** via 159 links — 6 new historical (`cardinal-wolsey` [the butcher's son's rise and fall, cited 43×], `the-kings-great-matter` [the divorce/Blackfriars trial, 38×], `the-break-with-rome` [Reformation/royal supremacy, 19×], `foxes-acts-and-monuments` [Foxe's Book of Martyrs, the Cranmer/Act-5 source, 13×], `cranmers-prophecy-of-elizabeth` [the 5.5 panegyric, 8×], `field-of-the-cloth-of-gold` [the 1520 summit, 1.1, 4×]) + 2 new biblical (`the-fall-of-the-angels` [Lucifer's pride — Wolsey's 'by that sin fell the angels', 3×], `queen-of-sheba` ['Saba', 5.5, 1×]) + reuse (`de-casibus-tragedy` 17 — the three falls of the mighty, plus `divine-right-of-kings`, `holinshed-chronicles`, `fortunes-wheel`, `cain-and-abel`, `the-kings-two-bodies`, `the-great-chain-of-being`) — `_review_queue/` cleared (21 rescued), **1000-entry glossary**. The late, **Fletcher-co-authored** outlier (the church-state pair's Reformation play); pageant-dense (4 long procession/vision/coronation/christening stage directions render fine). Heavy on `historical_topical` (121), `archaic_vocab` (80), `rhetorical_device` (52), `cultural_context` (22). Parser: needed a **gated speaker-less-prologue fix** (`THE PROLOGUE.` is followed straight by verse with no Chorus label — now attributed to a `Prologue` chorus speaker, gated on a `PROLOGUE.` alias / `hasPrologueSpeaker`; the bare `EPILOGUE` reads as an `Epilogue` chorus via the existing SONG_HEADER-alias mechanism) and a **gated inline-speaker split** (`NORFOLK. I thank your Grace,` opens a mid-verse-line, slug-gated `INLINE_SPEAKER_GUARD`). Speaker quirks: inconsistent periods (no-dot `BUCKINGHAM` ×7 in 1.1), `QUEEN KATHERINE.`/`KATHERINE.` one role, `BOTH.`/`ALL.` unison ensembles.
- **The Tempest** — 5 acts, 9 scenes, 2281 TLN'd lines; **247 annotations** (234 at build + 13 rescued from the review queue; 7 dropped, 1 anchor-unresolved of 255 candidates), wired to **9 reference cards** via 25 links — 8 new (classical: `juno-goddess`, `ceres-and-proserpina`, `iris-rainbow` [the 4.1 betrothal masque], `dido-queen-of-carthage` [2.1 'widow Dido' / 'Tunis was Carthage'], `the-harpy` [Ariel 'like a harpy', 3.3]; historical: `the-bermuda-wreck` [Strachey's *True Reportory* 1610, cited 4×], `montaigne-of-the-cannibals` [Gonzalo's 2.1 ideal-commonwealth, 9×], `setebos-patagonian-god` [Caliban's 'dam's god']) + reused `cupid-arrows` — `_review_queue/` cleared (13 rescued), **959-entry glossary**. First romance (Session 6) and the **risk-first masque validator: ZERO parser changes** — the masque's deity-speakers Iris/Ceres/Juno are just `characters.json` aliases, confirming deity-speaker handling works out of the box. Observes the unities (one afternoon, one island); light on biblical. Heavy on `archaic_vocab` (120, incl. nautical/magic), `cultural_context` (39, the New World/colonial material), `rhetorical_device` (32), `historical_topical` (22), `classical_allusion` (16), `wordplay` (14). The Epilogue (`EPILOGUE` header + `[Spoken by PROSPERO]`) attributes to Prospero (the last speaker) with no epilogue alias needed.
- **Pericles, Prince of Tyre** — 5 acts, 22 scenes, 2405 TLN'd lines; **248 annotations** (220 at build + 28 rescued from the review queue; 8 dropped, 1 anchor-unresolved of 257 candidates), wired to **10 reference cards** via 70 links — 5 new (historical: `gower-and-confessio-amantis` [the Presenter / medieval source poet, cited 28×], `apollonius-of-tyre` [the ur-romance via Twine, 3×]; classical: `neptune-god-of-the-sea` [the sea/storms, 6×; reusable Tempest/WT/Hamlet], `lucina-goddess-of-childbirth` [the births, 2×; reusable Cymbeline/Titus], `hesperides` [1.1 Antiochus's daughter as 'fair Hesperides']) + heavy reuse (`diana-goddess` 15 — Diana of Ephesus, Marina's chastity, the 5.1 theophany; `fortunes-wheel` 11; `juno-goddess`, `cupid-arrows`, `the-harpy`) — `_review_queue/` cleared (28 rescued), **863-entry glossary**. Co-authored with George Wilkins (Acts 1-2). The session's one gated parser extension: the **per-act Presenter (Gower) + dumb-shows** (Known limitations #9). Heavy on `archaic_vocab` (104, incl. Gower's deliberately Chaucerian verse), `cultural_context` (42), `rhetorical_device` (40), `classical_allusion` (38, the play's mythological density). Speaker quirks: typo-variants `PERACLES.`→Pericles, `HELCANUS.`/`HELICANE.`→Helicanus, `BARD.`→Bawd all aliased so no phantom speakers; the Mytilene brothel (4.2/4.5/4.6) and Antiochus's incest riddle (1.1) handled neutrally and with care.
- **Cymbeline** — 5 acts, 27 scenes, 3744 TLN'd lines; **386 annotations** (357 at build + 29 rescued from the review queue; 10 dropped, 3 anchor-unresolved of 396 candidates), wired to **14 reference cards** via 47 links — 4 new (classical `jupiter-king-of-gods` [the 5.4 theophany, cited 12×], `tarquin-and-lucrece` [Iachimo as Tarquin in the bedchamber, 2.2; reusable Macbeth/Lucrece]; historical `ancient-britain-and-the-roman-tribute` [Cymbeline/Cunobeline, Caesar's tribute, 13×], `milford-haven` [the convergence point + Tudor-landing resonance, 5×]) + reuse (`holinshed-chronicles` 4, `diana-goddess` 3, `juno-goddess` 2, `philomela`, `lucina-goddess-of-childbirth`, `neptune-god-of-the-sea`, `hercules`, `hecuba`, `cupid-arrows`, `sixth-commandment`) — `_review_queue/` cleared (29 rescued), **1054-entry glossary**. Heavy on `archaic_vocab` (160, the ornate late-romance verse), `rhetorical_device` (66), `syntax_grammar` (35), `historical_topical` (27), `classical_allusion` (26), `cultural_context` (22). **Source repair, NO parser change:** the vendored modern `cymbeline_gut.txt` was INCOMPLETE (missing Acts III-IV — the whole Welsh storyline; jumps 2.5→5.1). Per the user's decision, the complete public-domain **Moby XML** (`texts/moby/cymbeline_moby.xml`) was converted to standard `_gut.txt` form by `scripts/pipeline/_moby-to-gut.mjs` → committed `data/sources/cymbeline_gut.txt`, pointed to by a slug-gated `FULL_SOURCE_OVERRIDE` in `scripts/lib/paths.ts` (Known limitations #11). Jupiter's theophany (5.4) + the Leonati ghosts (Sicilius, Mother, two Brothers) are pure `characters.json` deity/ghost-speakers (the Tempest masque pattern); the unison labels `GUIDERIUS and ARVIRAGUS.`/`FIRST BROTHER and SECOND BROTHER.`/`CLOTEN and LORDS.` are aliased ensembles. Belarius=Morgan, the boys=Polydore/Cadwal, Imogen-as-Fidele all handled.
- **The Winter's Tale** — 5 acts, 15 scenes, 3233 TLN'd lines; **325 annotations** (305 at build + 20 rescued from the review queue; 5 dropped, 2 anchor-unresolved of 330 candidates), wired to **9 reference cards** via 20 links — 3 new (classical `apollo-and-the-delphic-oracle` [the oracle at 'Delphos' that vindicates Hermione, cited 8×], `flora-goddess-of-flowers` [Perdita as Flora, 4.4]; historical `giulio-romano` [the statue's maker, 5.2 — Shakespeare's only named Renaissance artist; authored but not yet linked]) + reuse (`fortunes-wheel` 4, `garden-of-eden` 2, `ceres-and-proserpina` [Perdita's Proserpina — the predicted S6→S7 reuse], `juno-goddess`, `neptune-god-of-the-sea`, `divine-right-of-kings`, `trial-by-combat`) — `_review_queue/` cleared (20 rescued), **1130-entry glossary**. Heavy on `archaic_vocab` (163), `rhetorical_device` (44), `cultural_context` (37, the sheep-shearing folk customs + Autolycus's peddler-cant), `syntax_grammar` (16, Leontes's knotted jealous verse), `classical_allusion` (15). **Time-as-Chorus needed NO parser change** (the user's hypothesis, confirmed in-browser): its SD `[Enter Time, as Chorus.]` does not trip the `enter chorus` buffer, and Time speaks within its own scene 4.1 as a plain `time` chorus character. `Exit, pursued by a bear` (3.3) reads as a `stage_direction_note`; the 3 false-positive speaker labels (`Sicilia.`/`Forbiddenly.`/`Neither.`) correctly stayed dialogue. Three Apollo/oracle/libation citations flagged by citation-check were given allowlisted ancient sources (Herodotus/Pausanias on Delphi; Homer/Virgil on libation).
- **The Taming of the Shrew** — 6 acts (a two-scene Induction = act 0, then 5 acts), 14 scenes, 2583 TLN'd lines; **238 annotations** (207 at build + 31 rescued from the review queue; 9 dropped, 5 anchor-unresolved of 247 candidates), wired to **11 reference cards** via 19 links — 5 new (classical `venus-and-adonis` [Adonis/Cytherea, the Induction painting; reusable for the poem], `daphne-and-apollo` [the Induction painting]; historical `falconry-and-the-haggard` [Petruchio's 4.1 taming-as-hawking conceit, cited 6×], `patient-griselda` [the obedient-wife type, 2.1], `the-sleeper-awakened` [the Sly Induction motif]) + reuse (`tarquin-and-lucrece`, `gower-and-confessio-amantis` [Florentius, 1.2], `dido-queen-of-carthage`, `diana-goddess`, `hercules`, `the-great-chain-of-being`) — `_review_queue/` cleared (31 rescued), **1016-entry glossary** (rich in Italian/Latin tags). Heavy on `archaic_vocab` (101), `cultural_context` (55, the taming + Induction frame), `wordplay` (25), `rhetorical_device` (18), `classical_allusion` (17). **The Christopher Sly INDUCTION** (two framing scenes before Act 1) is modeled as a **gated pseudo-act numbered 0**, titled `INDUCTION.`, rendered "Induction, Scene 1/2" with TLN 1 beginning in it (Known limitations #12); the schema now allows `act.number` 0 and the scene page labels act 0 "Induction." Xanthippe (1.2) prompted adding `Xenophon`/`Diogenes Laertius` to the citation allowlist.
- **Love's Labour's Lost** — 5 acts, 9 scenes, 2670 TLN'd lines; **243 annotations** (227 at build + 16 rescued from the review queue; 13 dropped, 6 anchor-unresolved of 256 candidates), wired to **12 reference cards** via 32 links — 4 new (classical `prometheus` [Berowne's 'Promethean fire', 4.3; reusable Othello]; historical `the-nine-worthies` [the 5.2 pageant], `the-masque-of-muscovites` [the 5.2 Russian disguise — authored but not linked, like `giulio-romano`]; biblical `samson-and-delilah` [Armado's 'great men in love', 1.2]) + heavy reuse (`cupid-arrows` 8, `hercules` 6, `petrarch-and-the-sonnet` 4, `pompey-the-great` 2, `diana-goddess` 2, `garden-of-eden` 2, `samson-and-delilah` 2, `prometheus` 2, `hesperides` [the 4.3 'Hesperides', predicted reuse], `nemean-lion`, `ate`) — `_review_queue/` cleared (16 rescued), **1040-entry glossary** (dense Latin/French tags incl. `honorificabilitudinitatibus`). The most language-dense comedy: heavy on `archaic_vocab` (72), `rhetorical_device` (49), `wordplay` (37), `historical_topical` (26), `classical_allusion` (23). **Act 2's lone scene is mislabeled `SCENE II.`** in the source — a **gated sequential scene-renumber** fixes it to 2.1; two `@@@` transcription-junk lines are skipped (Known limitations #13). The closing **Spring/Winter songs** and **Moth's `MOTH [Singing.]` Concolinel** are handled with `characters.json` no-period aliases (`SPRING`/`WINTER` = song personifications; `MOTH`); the Nine Worthies pageant needed no parser change (pure character data, like Midsummer's Pyramus).
- **The Comedy of Errors** — 5 acts, 11 scenes, 1740 TLN'd lines; **152 annotations** (141 at build + 11 rescued from the review queue; 7 dropped, 3 anchor-unresolved of 159 candidates), wired to **5 reference cards** via 39 links — 3 new (classical `plautus-and-roman-comedy` [the Menaechmi/Amphitruo source, cited 21×], `circe` [the 5.1 'Circe's cup']; biblical `st-paul-and-ephesus` [Ephesus as the city of magic (Acts 19) + the Pauline marriage code (Ephesians 5), 13×]) + reuse (`fortunes-wheel`, `the-prodigal-son`) — `_review_queue/` cleared (11 rescued), **888-entry glossary**. Shakespeare's shortest play; heavy on `archaic_vocab` (39), `cultural_context` (30), `wordplay` (25), `rhetorical_device` (16), `stage_direction_note` (13). **Twin-speaker disambiguation, NO parser change:** the two identical-twin pairs use full disambiguated labels (`ANTIPHOLUS OF SYRACUSE.`/`ANTIPHOLUS OF EPHESUS.`/`DROMIO OF SYRACUSE.`/`DROMIO OF EPHESUS.`, 18-23 chars) aliased to four distinct characters — accepted as exact aliases despite exceeding `looksLikeSpeaker`'s 18-char cap; a no-dot `BALTHAZAR` (source line 1042) needed a no-period alias. Arabic scene numbers (`SCENE 1.`) were already handled.
- **Coriolanus** — 5 acts, 29 scenes, 3641 TLN'd lines; **344 annotations** (295 at build + 49 rescued from the review queue; 23 dropped, 7 anchor-unresolved of 374 candidates), wired to **15 reference cards** via 125 links — 4 new historical (`the-fable-of-the-belly` [Menenius's 1.1 belly-and-members fable, cited 8×], `the-tribunes-of-the-people` [27×], `the-roman-consulship` [the 'gown of humility'; 'voices' = votes, 18×], `the-volscians-and-corioli` [Corioli/Antium/Aufidius's nation, 19×]) + heavy reuse of Julius Caesar's Roman/Plutarch bank (`plutarchs-lives` 32, `the-roman-triumph` 3, `tarquin-and-the-first-brutus` 3, `roman-republic-and-the-name-king` 2, `de-casibus-tragedy` 2, `the-great-chain-of-being`, `hercules`, `hecuba`, `fortunes-wheel` 2, plus `juno-goddess` 3 / `jupiter-king-of-gods` 3 for the play's Roman deities) — `_review_queue/` cleared (49 rescued, 0 cut), **1054-entry glossary**. First of the reuse-clustered remainder; standard structure, **NO parser change**. Heavy on `archaic_vocab` (136), `historical_topical` (85), `rhetorical_device` (67), `classical_allusion` (26). Source is Plutarch's Life of Coriolanus (North 1579) + Livy; the 1607 Midlands grain riots give the class conflict a topical charge. Speaker quirks: the ALL-CAPS `MARCIUS.` is the protagonist's Act-1 prefix (he is surnamed Coriolanus only at the end of Act 1) while the title-case `Marcius.` is a verse wrap-word that correctly stayed dialogue (do NOT alias it); Junius Brutus + Sicinius are the tribunes of the people (this Brutus is NOT the Brutus of Julius Caesar); a large minor cast (51 roles incl. numbered citizens/senators/servingmen/conspirators/lords + ensembles).
- **Titus Andronicus** — 5 acts, 14 scenes, 2542 TLN'd lines; **232 annotations** (215 at build + 17 rescued from the review queue; 10 dropped, 4 anchor-unresolved of 246 candidates), wired to **16 reference cards** via 60 links — **the Ovid bank** authored here: 1 new classical (`ovids-metamorphoses` [Golding 1567; the book carried on stage in 4.1, 4×]) + 3 new historical (`seneca-and-revenge-tragedy` [the Thyestean cannibal banquet, 12×], `the-moor-on-the-english-stage` [Aaron's racialization, handled as context not endorsement, 12×], `virginius-and-virginia` [Titus's stated 5.3 model for killing Lavinia]) + heavy reuse (`philomela` 9 — Lavinia as Philomela AND Titus's banquet as Procne's; `tarquin-and-lucrece` 4 — Lavinia as Lucrece; `fall-of-troy` 3, `the-vice` 3, `dido-queen-of-carthage` 2, `the-stage-machiavel` 2, `hecuba` 2, `diana-goddess` 2, `astraea`, `hyperion`, `prometheus`, `the-roman-triumph`) — `_review_queue/` cleared (17 rescued, 0 cut), **1028-entry glossary**. Standard structure, **NO parser change** (Arabic `ACT 1.` headers already handled). The canon's most violent play: the rape/mutilation of Lavinia, the racism around Aaron, and the cannibal banquet are annotated clinically and neutrally per docs/CONTENT_STANDARDS.md — `the-moor-on-the-english-stage` frames the anti-Black language as the *characters'*, with Aaron's 'is black so base a hue?' / 'Coal-black is better than another hue' as its counter-voice (typed `cultural_context`, citing Kim Hall's *Things of Darkness*). Heavy on `archaic_vocab` (72), `classical_allusion` (53, the Ovidian density), `rhetorical_device` (34), `cultural_context` (25). Speaker note: `MARTIUS.` is Titus's son (NOT Coriolanus's Marcius); the silent roles Alarbus and Aaron's child are included as character data. Two cosmetic post-ingest setting cleanups (2.2 dangling 'Horns', 5.2 stray ']'). The classical-citation allowlist gained `Diodorus Siculus` (the chief ancient source for Semiramis, 2.1/4.1).
- **Antony and Cleopatra** — 5 acts, 42 scenes, 3518 TLN'd lines; **378 annotations** (310 at build + 68 rescued from the review queue; 18 dropped, 3 anchor-unresolved of 396 candidates), wired to **18 reference cards** via 138 links — the **Antony and Cleopatra delta** (Egyptian/battle/triumvirate): 5 new historical (`the-second-triumvirate` [the three world-sharers; 'triple pillar of the world', cited 32×], `the-ptolemies-and-egypt` [Egypt vs Rome, the East, 18×], `the-battle-of-actium` [the sea-fight thrown away when Cleopatra flees, 17×], `the-asp` [the 'worm of Nilus', Cleopatra's death, 2×], `augustus-and-the-pax-romana` [Octavius → Augustus, the 4.6 'universal peace', 3×]) + 2 new classical (`cleopatra-as-isis` [the goddess-queen, 3.6, 7×], `the-barge-on-the-cydnus` [Enobarbus's 2.2 set-piece, following North's Plutarch nearly verbatim, 5×]) + heavy reuse of the Roman/Plutarch bank (`plutarchs-lives` 14, `the-antique-roman` 8 — the 'high Roman fashion' suicides of Antony/Eros/Cleopatra/Charmian, `pompey-the-great` 7 — the dead father vs the son Sextus, `the-roman-triumph` 6 — Cleopatra refusing to grace it, `fortunes-wheel` 6, `roman-augury` 5 — the Soothsayer, `hercules` 3 — the god deserting Antony 4.3, `lethe` 2, `dido-queen-of-carthage`, `jupiter-king-of-gods`, `juno-goddess`) — `_review_queue/` cleared (68 rescued, 0 cut), **996-entry glossary**. The canon's most scene-heavy play (solo build, budgeted as a heavy two-play session: ~126 annotator+judge agents in one heavy annotate). Heavy on `historical_topical` (118), `archaic_vocab` (114), `rhetorical_device` (53), `classical_allusion` (48), `cultural_context` (17). Source is Plutarch's Life of Antony (North 1579) — Enobarbus's Cydnus barge speech follows North nearly verbatim. **First plays-wide parser change since Session 8 (two general, byte-identical fixes):** A&C is the first play with an act of more than ten scenes (Act 3 has 13, Act 4 has 15), so `toInt` needed proper Roman-numeral parsing (the old I..X lookup table returned 0 for XI-XV, silently merging those scenes' text into scene X — the initial ingest yielded only 33 of 42 scenes); and its Act 4 opens with a combined `ACT IV. SCENE I.` header on one line (its other four acts use a separate `ACT n.` line), now split into two canonical lines before parsing. Both proven byte-identical for all 23 prior plays by an original-vs-patched re-ingest hash diff (Known limitations #14). Speaker quirks: `CAESAR.` = Octavius (the living triumvir, later Augustus), NOT the invoked-but-absent Julius Caesar; `POMPEY.` = Sextus Pompeius (the young Pompey), his father Pompey the Great only invoked; the OCR typo `THYMUS.` (3.13) is aliased to Thyreus; the `SONG.` Bacchanal (2.7) is a `song` ensemble character; eleven title-case verse wrap-words (`Dead.`, `Come.`, `Not I.`, etc.) correctly stayed dialogue (the Coriolanus `Marcius.` lesson); 56 roles incl. numbered soldiers/guards/servants/attendants/messengers. Two annotator-emitted `detail: null` fields (×18) were stripped post-merge to satisfy the schema.
- **Macbeth** — 5 acts, 26 scenes, 2349 TLN'd lines; **233 annotations** (208 at build + 25 rescued from the review queue; 12 dropped, 4 anchor-unresolved of 249 candidates), wired to **17 reference cards** via 79 links — the **Macbeth delta** (witchcraft / 1605-06 topical): 4 new historical (`the-weird-sisters-and-witchcraft` ['wyrd' = fate; James I's *Daemonologie* + the 1604 witch-statute, cited 14×], `equivocation-and-the-gunpowder-plot` [the Porter's 'equivocator' 2.3 + Macbeth's 'equivocation of the fiend' 5.5; the Jesuit Henry Garnet's 1606 trial, 8×], `the-kings-evil` [the 4.3 royal touch for scrofula, a compliment to James, 4×], `banquo-and-the-stuart-line` [the 4.1 show of eight kings; James's claimed descent from Banquo via Fleance, 4×]) + reuse of the history "spine" (`holinshed-chronicles` 17, `the-great-chain-of-being` 8, `divine-right-of-kings` 5, `de-casibus-tragedy` 3, `the-kings-two-bodies` 2, `fortunes-wheel` 2) + `triple-hecate` 5 (Hecate) + biblical `pilate-washing-hands` 2 / `cain-and-abel` 1 (Duncan's murder; Lady Macbeth's hand-washing) + opportunistic `tarquin-and-lucrece`/`neptune-god-of-the-sea`/`plutarchs-lives`/`the-antique-roman` (1 each) — `_review_queue/` cleared (25 rescued, 0 cut), **894-entry glossary**. The most-taught tragedy (difficulty 3); standard roman-numeral structure, **NO parser change**. Heavy on `archaic_vocab` (98), `historical_topical` (44), `rhetorical_device` (29), `cultural_context` (17), `biblical_allusion` (16), `classical_allusion` (11); confidence high 203 / medium 29 / uncertain 1. Source is Holinshed's *Chronicles* (Scotland, after Hector Boece). **Edition note:** the vendored modern text divides Act 2 into only TWO scenes — its `SCENE I` merges the standard 2.1-2.3 (Banquo/Fleance + the dagger soliloquy, Duncan's murder, the Porter, the discovery) and its `SCENE II` is the Ross/Old-Man scene (standard 2.4) — hence 26 scenes, not the Folger/MIT 28 (faithful to the source; TLN is continuous, so annotations are unaffected). Speaker handling, pure `characters.json` (41 roles): the bare `DOCTOR.` prefix covers BOTH the English Doctor (4.3, the King's-Evil speech) and the Scottish Doctor of Physic (5.1 sleepwalking, 5.3) — one shared-prefix role per the runbook rule; the three `APPARITION.` spirits (4.1, incl. a glued `APPARITION.--`) are one `apparition` role; the title-case `Song.` cue (4.1 'Black spirits', a likely Middleton interpolation) is aliased to a `song` ensemble (the 3.5 'Come away' cue is a bracketed SD, no alias needed); `SIWARD.`/`YOUNG SIWARD.` distinct; nine title-case wrap-words (`Now.`, `Ay.`, `Donalbain.`, `Speak.`, etc.) correctly stayed dialogue. Regicide, the murder of Lady Macduff and her son (4.2), Lady Macbeth's sleepwalking and suicide, and witchcraft are annotated clinically per docs/CONTENT_STANDARDS.md.
- **King Lear** — 5 acts, 26 scenes, 3390 TLN'd lines; **364 annotations** (323 at build + 41 rescued from the review queue; 10 dropped, 2 anchor-unresolved of 376 candidates), wired to **16 reference cards** via 81 links — the **King Lear delta**: 5 new historical (`harsnetts-declaration-and-the-devils` [Poor Tom's fiends Flibbertigibbet/Smulkin/Modo from Samuel Harsnett's 1603 *Declaration of Egregious Popish Impostures*, cited 9×], `king-leir-and-legendary-britain` [Geoffrey of Monmouth / Holinshed + the old *King Leir* play; Shakespeare's tragic ending vs the sources' happy one, and Tate's 1681 rewrite, 7×], `astrology-and-the-eclipses` [Gloucester's 1.2 'late eclipses' vs Edmund's mockery; the 1605 eclipses, 5×], `dover-and-the-cliff` [4.6, Edgar gulling the blind Gloucester, 4×], `the-division-of-the-kingdom` [the 1.1 map/partition + the Jacobean Union resonance, 2×]) + reuse of the history "spine" (`the-great-chain-of-being` 15, `the-kings-two-bodies` 9, `de-casibus-tragedy` 4, `trial-by-combat` 3 — Edgar vs Edmund 5.3, `divine-right-of-kings` 1) + `the-stage-machiavel` 7 (Edmund) + `fortunes-wheel` 7 ('the wheel is come full circle') + the pagan-gods classical bank (`jupiter-king-of-gods` 4, `triple-hecate` 2, `apollo-and-the-delphic-oracle` 1) + biblical `garden-of-eden` 1 — `_review_queue/` cleared (41 rescued, 0 cut), **1056-entry glossary**. The canon's bleakest tragedy (difficulty 5); standard structure, **NO parser change** (title-case `Scene I.` headers parse via the case-insensitive header regex). Heavy on `archaic_vocab` (150), `historical_topical` (59), `rhetorical_device` (59), `cultural_context` (43), `classical_allusion` (17); confidence high 325 / medium 33 / uncertain 6; 2 `textual_variant` (the Q/F two-text differences). **Edition note:** the vendored text is the **conflated Quarto+Folio** (Globe-style) edition — it carries both the Quarto-only mock-trial (3.6, the joint-stools 'arraigned' as Goneril and Regan) and the Quarto-only Kent/Gentleman scene (4.3) — the fullest teaching text. Title-case abbreviated speaker prefixes (like Hamlet): all 30 printed labels aliased to 26 roles, verified complete by a per-character speech-count check against `extract-speakers` (e.g. `Cor.` = CORDELIA, only 1.1 + Acts 4-5; `Corn.` = CORNWALL, only Acts 2-3 — never confused; Edgar = 'Poor Tom' and Kent = 'Caius' are each ONE role since the source keeps one prefix; `Alb. and Corn.` is a small unison ensemble); twelve title-case wrap-words (`Nothing.`, `Service.`, `Authority.`, etc.) correctly stayed dialogue. The on-stage blinding of Gloucester (3.7), filial cruelty, Lear's madness, Gloucester's attempted Dover suicide (4.6), and the deaths of Cordelia and Lear are annotated clinically per docs/CONTENT_STANDARDS.md.
- **Much Ado About Nothing** — 5 acts, 17 scenes, 2254 TLN'd lines; **231 annotations** (196 at build + 35 rescued from the review queue; 6 dropped, 1 anchor-unresolved of 238 candidates), wired to **13 reference cards** via 81 links — the **Much Ado delta**: 4 new historical (`the-cuckolds-horns` [the horn/cuckold jokes and male honour anxiety; the Actaeon root, cited 12×], `nothing-and-noting` [the title's triple pun — 'nothing'≈'noting' (overhearing) + the bawdy 'no-thing', 16×], `chastity-and-the-double-standard` [a woman's honour = chastity; Hero's slander, via Vives 1529, 11×], `the-constable-and-the-watch` [Dogberry/Verges and the Elizabethan watch; the malapropisms, via Lambarde 1582, 17×]) + 1 new classical (`hero-and-leander` [Benedick's 5.2 'Leander the good swimmer'; Marlowe 1598 / Ovid / Musaeus]) + reuse of the comedy/love banks (`cupid-arrows` 6, `petrarch-and-the-sonnet` 5, `the-stage-machiavel` 5 [Don John the bastard malcontent], `hercules` 3 [2.1 'made Hercules have turned spit'], `diana-goddess` 2, plus the annotators' own finds `fama-rumour-personified`, `ate`, `the-harpy` [Benedick's 2.1 'harpy']) — `_review_queue/` cleared (35 rescued, 0 cut), **922-entry glossary**. First of the festive comedies; prose-heavy (Beatrice/Benedick's wit, Dogberry), renders fine. Heavy on `archaic_vocab` (59), `wordplay` (54), `cultural_context` (41), `rhetorical_device` (26), `classical_allusion` (19), `historical_topical` (18), `bawdy_pun` (9); confidence high 200 / medium 29 / uncertain 2. Source: Bandello (the Hero–Claudio plot) + Ariosto (Ariodante/Ginevra); Beatrice/Benedick and Dogberry are Shakespeare's own. **Gated parser fix:** the first song 'Sigh no more, ladies' (2.3) is introduced by an `[Balthasar sings.]` stage direction with unlabeled lyrics — a slug-gated rule (`SINGER_SD_SETS_SPEAKER`) sets the singer named in a `[X sings.]` direction as the current speaker, so the song attributes to Balthasar, not the prior speaker Benedick (the second song, 'Pardon, goddess of the night' 5.3, has its own `SONG.` prefix → a `song` ensemble); proven byte-identical for all prior plays (incl. the Tempest's Ariel songs) by an original-vs-patched re-ingest hash diff. Speaker handling, pure `characters.json` (25 roles): `BOTH.` (4.2 'Yea, sir, we hope') is a Borachio+Conrade unison ensemble, `A LORD.` (5.3) a minor role; the title-case dialogue lines `Borachio.`/`Beatrice.`/`Amen.`/`Well.`/`Kill Claudio.`/`Italy.` correctly stayed dialogue. The public slander and shaming of Hero (4.1), the cuckold humour, and Beatrice's 'Kill Claudio' are annotated clinically per docs/CONTENT_STANDARDS.md.
- **Twelfth Night, or What You Will** — 5 acts, 18 scenes, 2326 TLN'd lines; **231 annotations** (212 at build + 19 rescued from the review queue; 13 dropped, 5 anchor-unresolved of 249 candidates), wired to **14 reference cards** via 30 links — the **Twelfth Night delta**: 3 new historical (`twelfth-night-and-the-lord-of-misrule` [the Epiphany revels / Saturnalia inversion the play is named for; 'cakes and ale', via Stubbes 1583, cited 6×], `puritanism-and-revelry` [Malvolio 'a kind of Puritan'; killjoy-vs-festivity, 6×], `illyria` [the remote, piratical Adriatic setting]) + 3 new classical (`arion-and-the-dolphin` [1.2 'like Arion on the dolphin's back', via Herodotus], `elysium` [1.2 'My brother he is in Elysium' / the Illyria pun], `pythagoras-and-the-transmigration-of-souls` [4.2 Sir Topas on 'wild fowl', via Ovid Met. 15; reusable for Merchant]) + reuse (`petrarch-and-the-sonnet` 5 [Orsino the Petrarchan lover], `tarquin-and-lucrece` 2 [Malvolio's 2.5 Lucrece seal], `fortunes-wheel` 2, `cupid-arrows`, `diana-goddess`, `lethe` [4.1 'Lethe steep'], `falconry-and-the-haggard` [3.1 'like the haggard'], biblical `garden-of-eden`) — `_review_queue/` cleared (19 rescued, 0 cut), **962-entry glossary**. The second festive comedy (a common curriculum text); prose-and-verse, renders fine. Heavy on `archaic_vocab` (83), `historical_topical` (43), `cultural_context` (39), `wordplay` (19), `rhetorical_device` (17), `classical_allusion` (14), `biblical_allusion` (8), `bawdy_pun` (7); confidence high 208 / medium 19 / uncertain 4. Source: Riche's *Apolonius and Silla* (1581) + the anonymous Italian *Gl'Ingannati* (1531). **Gated parser fix:** the closing song 'When that I was and a little tiny boy' (5.1) is cued by a title-case `Song.` line (the other two songs, 2.3 'O mistress mine' and 2.4 'Come away death', use the all-caps `SONG` that `SONG_HEADER` already catches) — a slug-gated rule (`TITLE_SONG_CUE`) treats a title-case `Song.` as a header, so the song stays Feste's with no stray 'Song.' line; proven byte-identical for prior plays (Macbeth's aliased title-case `Song.` untouched, the lowercase 'air.' in the unbuilt Two Gentlemen avoided) by re-ingest hash diff. Speaker handling, pure `characters.json` (18 roles): Viola is disguised throughout as the page 'Cesario' (ONE `viola` role), her twin Sebastian a distinct role (resemblance, not a shared prefix), `CLOWN.`=Feste, `DUKE.`=Orsino; this play's Antonio (a sea-captain) is a DIFFERENT character from Much Ado's. Sir Andrew's FIRST speech-prefix is the hyphenated `AGUE-CHEEK.` (his surname; source line 295) — the shape heuristic rejects the hyphen, so an exact alias was added (else his line folded into Sir Toby). The cruel gulling and dark-room imprisonment of Malvolio (2.5–5.1), and the gender/cross-dressing and same-sex desire around Viola-Cesario/Orsino/Olivia and Antonio/Sebastian, are annotated clinically per docs/CONTENT_STANDARDS.md.
- **As You Like It** — 5 acts, 22 scenes, 2512 TLN'd lines; **251 annotations** (231 at build + 20 rescued from the review queue; 4 dropped, 1 anchor-unresolved of 256 candidates), wired to **19 reference cards** via 54 links — the **As You Like It delta**: 4 new historical (`the-pastoral-and-the-forest-of-arden` [the pastoral 'green world' + Lodge's *Rosalynde*; the court-vs-country debate, cited 5×], `melancholy-and-the-malcontent` [Jaques as the Galenic-melancholy malcontent; Bright 1586, 7×], `robin-hood-and-the-greenwood` [Duke Senior's band 'like the old Robin Hood', 2×], `the-seven-ages-of-man` [Jaques's 2.7 'All the world's a stage' + the theatrum mundi, 2×]) + 3 new classical (`ganymede` [Rosalind's disguise-name; Jove's cupbearer + the boy-actor/same-sex resonance, 6×], `the-golden-age` [Duke Senior's 'golden world'; Ovid Met. 1, 4×], `hymen-god-of-marriage` [the 5.4 wedding masque, 1×]) + reuse of the comedy/love banks (`petrarch-and-the-sonnet` 7 [Orlando's tree-verses, the Petrarchan-lover satire], `the-cuckolds-horns` 4 [the 4.2 horn song + Touchstone's horn jokes], `cupid-arrows` 4, `diana-goddess` 3, `hero-and-leander` 2 [Rosalind's 4.1 'Leander', Phebe's 3.5 Marlowe quote], `ovids-metamorphoses` 1 [Touchstone's 3.3 'Ovid among the Goths'], `hercules`/`fortunes-wheel`/`the-roman-triumph`/`juno-goddess` 1 each, biblical `providence-and-the-sparrow`/`the-prodigal-son` 1) — `_review_queue/` cleared (20 rescued, 0 cut), **1018-entry glossary**. Common curriculum text (difficulty 3, `common_in_curriculum` true). Heavy on `archaic_vocab` (81), `cultural_context` (44), `wordplay` (29), `rhetorical_device` (28), `classical_allusion` (27), `historical_topical` (21), `biblical_allusion` (10), `bawdy_pun` (8), `parallel_passage` (1 — Phebe quoting Marlowe's *Hero and Leander*, re-typed from `classical_allusion` since Marlowe is not an allowlisted ancient author); confidence high 212 / medium 38 / uncertain 1. Source: Lodge's *Rosalynde: Euphues' Golden Legacy* (1590). **Gated parser fix:** 'Blow, blow, thou winter wind' (2.7) is cued by `[AMIENS sings.]` then a `SONG` header + unlabeled lyrics that would fold into the prior speaker (Duke Senior) — the `SINGER_SD_SETS_SPEAKER` rule (widened from Much Ado to this slug) attributes them to Amiens; its two bare-`SONG.` songs (the 4.2 horn song, the 5.3 pages' song) go to a `song` ensemble (alias `SONG.`), while 2.5 'Under the greenwood tree' and the 5.4 Hymen song attribute via their own `AMIENS.`/`HYMEN.` prefixes; proven byte-identical for all 28 prior plays by the original-vs-patched re-ingest hash diff. Speaker handling, pure `characters.json` (27 roles): TWO distinct Dukes (`DUKE SENIOR.` the banished, `DUKE FREDERICK.` the usurper) and TWO distinct Jaques (`JAQUES.` the melancholy lord, `JAQUES DE BOIS.` Orlando's brother) — never conflated; Rosalind-as-'Ganymede' and Celia-as-'Aliena' are each ONE role (the prefix never changes); the Roman-numeral verse markers `I.`/`II.`/`III.`/`IV.` are caught by the existing `VERSE_NUMERAL` rule, and the title-case wrap-words `Orlando.`/`Proceed.`/`Audrey.`/`Nor I.` correctly stayed dialogue. The gender/cross-dressing and same-sex resonance (Rosalind-as-Ganymede wooing Orlando; the 'If I were a woman' epilogue) is annotated clinically per docs/CONTENT_STANDARDS.md.
- **The Two Gentlemen of Verona** — 5 acts, 20 scenes, 2200 TLN'd lines; **229 annotations** (206 at build + 23 rescued from the review queue; 6 dropped, 3 anchor-unresolved of 238 candidates), wired to **13 reference cards** via 42 links — the **Two Gentlemen delta**: 1 new classical (`proteus-the-shape-changer` [the inconstant gentleman's name; the shape-shifting sea-god of Homer/Virgil, cited 7×]) + 1 new historical (`the-renaissance-friendship-ideal` [the amicitia / Titus-and-Gisippus ideal behind Valentine's startling 5.4 offer; Cicero/Elyot 1531, 2×]) + reuse (`petrarch-and-the-sonnet` 15 [the love-letters + Petrarchan wooing], `robin-hood-and-the-greenwood` 5 [the forest outlaws 4.1/5.4 'as the old Robin Hood of Sherwood' — forward-seeded from As You Like It], `cupid-arrows` 4, `hero-and-leander` 2 [the 1.1 'Leander cross'd the Hellespont'], `phaethon` 1 [the Duke's 3.1 'Phaethon, for thou art Merops' son'], `diana-goddess`/`fortunes-wheel`/`elysium`/`philomela`/`shrift-and-confession`/biblical `the-prodigal-son` 1 each) — `_review_queue/` cleared (23 rescued, 0 cut), **835-entry glossary**. Shakespeare's earliest comedy (difficulty 3, early/lesser-taught — `common_in_curriculum` false). Heavy on `archaic_vocab` (90), `cultural_context` (46), `wordplay` (31), `rhetorical_device` (29), `classical_allusion` (16), `historical_topical` (7), `bawdy_pun` (5); confidence high 197 / medium 32. Source: Montemayor's *Diana Enamorada* (the Felix/Felismena story) + Elyot's *Titus and Gisippus*. **TWO gated parser fixes:** (a) `SONG_SD_SETS_SINGER` — the serenade 'Who is Silvia?' (4.2) is cued by a bracketed `[SONG]` with unlabeled lyrics that would fold into the bystander Host (who has just said 'let's hear 'em'); the rule routes them to a `Musicians` ensemble (alias `SONG.`); (b) a slug-gated **inline/bracketed speaker-label splitter** — this early edition prints the 1.1 'nod/ay/noddy' stichomythia and several asides with the speaker label INLINE (bracketed `[SPEED] Ay.` or dotted `PROTEUS. Nod, ay?...`), so a leading known-alias label is split onto its own line and the standard prefix path attributes it (without it, `[SPEED]` was read as a stage direction and 'Ay.' became a phantom speaker — caught by `_verify-ingest.mjs`'s unmapped-speaker check). Both proven byte-identical for all 28 prior plays by the original-vs-patched re-ingest hash diff. Speaker handling, pure `characters.json` (20 roles): Julia-as-page-'Sebastian' is ONE `julia` role (a DIFFERENT Sebastian from Twelfth Night's twin); Crab the dog is a silent non-speaking role; the bare `OUTLAW.` (5.4 'A prize, a prize!') is a generic band member alongside `FIRST/SECOND/THIRD OUTLAW.`; nine title-case wrap-words (`Nothing.`, `Neither.`, `Valentine.`, `Banished Valentine.`, etc.) correctly stayed dialogue. The troubling final scene — Proteus's attempted assault on Silvia (5.4) and Valentine's 'All that was mine in Silvia I give thee' — is annotated clinically per docs/CONTENT_STANDARDS.md, presenting the textual/critical problem without endorsement.
- **The Merchant of Venice** — 5 acts, 20 scenes (Act 2 = 9 scenes), 2602 TLN'd lines; **254 annotations** (226 at build + 28 rescued from the review queue; 12 dropped, 7 anchor-unresolved of 266 candidates), wired to **18 reference cards** via 70 links — the **Merchant delta**: 4 new historical (`the-jew-on-the-english-stage` [the content-care anchor — the usurer-'Jew' stereotype, Marlowe's Barabas, the 1290 expulsion + the 1594 Lopez affair; PARALLEL to `the-moor-on-the-english-stage`, cited 22×], `usury-and-the-bond` [Christian-vs-Jewish lending, Aristotle's 'barren metal', the 1571 statute; the flesh-bond, 7×], `the-three-caskets` [gold/silver/lead; source the Gesta Romanorum / Robinson 1577, 7×], `the-quality-of-mercy` [Portia's 4.1 plea; mercy/justice + equity-vs-law, 3×]) + 1 new biblical (`jacob-and-laban` [Genesis 30:25-43; Shylock's 1.3 defence of interest, 2×]) + reuse (`the-renaissance-friendship-ideal` 9 [Antonio/Bassanio — forward-seeded from Two Gentlemen], `the-moor-on-the-english-stage` 5 [the Prince of Morocco], `hercules` 3 ['young Alcides', 3.2], `diana-goddess`/`the-prodigal-son` 2 each, `pythagoras-and-the-transmigration-of-souls` 1 [Gratiano's 4.1 'currish spirit... govern'd a wolf'], `cupid-arrows`/`fortunes-wheel`/`pyramus-thisbe`/`dido-queen-of-carthage`/`erebus`/`cato-the-younger` 1 each) — `_review_queue/` cleared (28 rescued, 0 cut), **870-entry glossary**. Much-taught but complex (difficulty 4, `common_in_curriculum` true). Heavy on `archaic_vocab` (74), `historical_topical` (51), `cultural_context` (38), `classical_allusion` (24), `wordplay` (20), `biblical_allusion` (20), `rhetorical_device` (14), `bawdy_pun` (8); confidence high 214 / medium 33 / uncertain 7. Sources: Fiorentino's *Il Pecorone* (the bond) + the Gesta Romanorum (the caskets). **Gated parser fix:** `SONG_SD_DESC_SETS_SINGER` — the song 'Tell me where is fancy bred' (3.2) is cued by a DESCRIPTIVE bracketed `[A Song, whilst BASSANIO comments on the caskets to himself.]` (no singer named) whose lyrics would otherwise fold into the prior speaker (Portia); a stage direction beginning 'A Song' routes them to a `musicians` ensemble (alias `SONG.`); proven byte-identical for all 30 prior shipped plays by the original-vs-patched re-ingest hash diff (known-noise only R2/H4.1/Titus). Speaker handling, pure `characters.json` (22 roles): the Prince of Morocco's multi-word prefix `PRINCE OF MOROCCO.` (+ the mixed-case `PRINCE OF Morocco.` variant) aliased exactly — `extract-speakers` misses multi-word labels, so the post-ingest unmapped check is what confirms it; this edition uses only `SALARINO.`/`SALANIO.` (no third 'Salerio' spelling); `CLERK.` is the court clerk who reads Bellario's letter (4.1), DISTINCT from Nerissa's law-clerk disguise; `BALTHASAR.` is Portia's servant (3.4), distinct from her dialogue-only lawyer disguise-name 'Balthazar'; Portia and Nerissa keep their own prefixes throughout the trial even while disguised; the false-positive prefix 'Master Launcelot.' (the tail of Launcelot's wrapped 2.2 line) correctly stayed dialogue. The antisemitism (Shylock's forced conversion 4.1, 'Hath not a Jew eyes' 3.1, Jessica's elopement) is annotated clinically per docs/CONTENT_STANDARDS.md, framed via `the-jew-on-the-english-stage` and presenting the textual/critical problem without endorsement.
- **Measure for Measure** — 5 acts, **18 scenes** (this Globe/Cambridge edition splits Act 1 into 5 scenes — a separate 1.2 'A street' and 1.3 'The same' where MIT runs one scene — vs MIT's 17), 2715 TLN'd lines; **290 annotations** (269 at build + 21 rescued from the review queue; 8 dropped, 2 anchor-unresolved of 298 candidates), wired to **12 reference cards** via 97 links — the **Measure delta**: 4 new historical (`the-disguised-ruler` [the Duke-as-friar 'disguised ruler' play, c. 1603-04; Marston/Middleton + James I's Basilikon Doron, cited 13×], `the-precise-puritan` [Angelo 'precise', Lucio's 'snow-broth'; cf. Malvolio, 14×], `the-bawdy-houses-of-vienna` [1.2 pulling down the suburb stews; the Bankside brothels, 14×], `the-bed-trick` [Mariana's substitution; Cinthio/Whetstone; cf. All's Well, 11×]) + 1 new biblical (`measure-for-measure-sermon-on-the-mount` [the title — Matthew 7:1-2 'with what measure ye mete'; the 5.1 'measure still for measure', 13×]) + reuse (`shrift-and-confession` 12 [the friar hearing confession], `chastity-and-the-double-standard` 11 [Isabella's 'more than our brother is our chastity'], `the-constable-and-the-watch` 4 [Elbow], `pompey-the-great` 2, `the-vice`/`jupiter-king-of-gods`/`the-roman-triumph` 1 each) — `_review_queue/` cleared (21 rescued, 0 cut), **877-entry glossary**. A problem play, college-level (difficulty 4, `common_in_curriculum` false). Heavy on `archaic_vocab` (100), `historical_topical` (53), `cultural_context` (41), `rhetorical_device` (31), `wordplay` (27), `bawdy_pun` (17), `biblical_allusion` (10), `syntax_grammar` (7), `classical_allusion` (4); confidence high 246 / medium 43 / uncertain 1. Sources: Whetstone's *Promos and Cassandra* (1578) + Cinthio's *Hecatommithi* (1565). **ONE gated parser fix:** `UNISON_AND_UNDOTTED` — the 5.1 unison cue `ANGELO and ESCALUS.` (leading name undotted) misses the dotted-parts unison test, and the 19-char line then trips the single-name length cap and is read as dialogue, injecting a stray label into the Duke's speech; the rule accepts the 'NAME and NAME.' form when both are known aliases and joins the proper names ('Angelo and Escalus'), exactly as the dotted-parts path does for JC's 'Titinius and Messala'; proven byte-identical for all 30 prior shipped plays by the re-ingest hash diff. The 4.1 song 'Take, O take those lips away' needed **NO** parser change — the singing Boy is a `boy` character aliased `SONG`/`SONG.`, so its all-caps `SONG` header attributes the unlabeled lyrics via the existing aliased-header speaker path (as As You Like It's bare-`SONG.` ensemble does), and without it the lyrics would have been dropped (curSpeaker is null at scene-open). Speaker handling, pure `characters.json` (24 roles): Duke Vincentio = `DUKE.` throughout, even disguised as 'Friar Lodowick' (dialogue-only); Pompey the bawd/clown = `CLOWN.` (and 'Pompey.' / 'Mistress Overdone.' at 1.2 are his DIALOGUE answers, not prefixes); Mistress Overdone = `BAWD.`; two distinct friars (`FRIAR.` = Thomas in 1.3, `PETER.` = Peter in 5.1); the title-case single-word 'phantoms' flagged by `extract-speakers` (`Amen.`, `Judge.`, `Yea.`, `True.`, `Proof.`, `Right.`, `Mum.`, `Mutually.`) are all in-block dialogue replies and correctly stayed dialogue (none leaked, per `_verify-ingest.mjs`). The sexual coercion (Angelo's blackmail of Isabella 2.4 'more than our brother is our chastity', the bed-trick substitution of Mariana) is annotated clinically per docs/CONTENT_STANDARDS.md, framed via `the-bed-trick`/`chastity-and-the-double-standard` and presenting the textual/critical problem without endorsement.
- **All's Well That Ends Well** — 5 acts, 23 scenes, 2764 TLN'd lines; **274 annotations** (248 at build + 26 rescued from the review queue; 13 dropped, 4 anchor-unresolved of 265 candidates), wired to **11 reference cards** via 84 links — the **All's Well delta**: 2 new historical (`giletta-of-narbonne` [the Boccaccio Decameron 3.9 / Painter Palace of Pleasure 1566 source + the 'clever wench / impossible tasks' folktale, cited 16×], `the-kings-fistula-and-the-healer` [Helena's cure; empirics vs the College of Physicians + the wonder-cure, 9×]) + 1 new classical (`the-miles-gloriosus` [Parolles the braggart-coward; Plautus's Miles Gloriosus, 21×]) + reuse of forward-seeded/bank cards (`the-bed-trick` 13 [Helena-for-Diana — forward-seeded from Measure], `chastity-and-the-double-standard` 11 [the 1.1 virginity debate], `palmers-and-pilgrims` 5 [Helena's pilgrimage to Saint Jaques le Grand], `fortunes-wheel` 3, `diana-goddess` 3, `helen-of-troy`/`juno-goddess`/`hercules` 1 each) — `_review_queue/` cleared (26 rescued, 0 cut), **981-entry glossary**. A problem play, college-level (difficulty 4, `common_in_curriculum` false). Heavy on `archaic_vocab` (98), `cultural_context` (47), `historical_topical` (41), `rhetorical_device` (30), `classical_allusion` (17), `wordplay` (17), `bawdy_pun` (10), `syntax_grammar` (6), `biblical_allusion` (5); confidence high 256 / medium 18. Source: Boccaccio (Decameron 3.9, Giletta of Narbonne) via William Painter's *Palace of Pleasure* (1566). **NO parser change** — the 1.3 Clown song 'Was this fair face the cause' is properly prefixed `CLOWN.` then `[Sings.]`, so the standard speaker path attributes it (the `SINGER_SD_SETS_SPEAKER` rule is gated to much_ado/as_you_like_it, so it never fires here). Speaker handling, pure `characters.json` (24 roles): the two French lords (the brothers Dumaine) `FIRST LORD.`/`SECOND LORD.` double as the eligible bachelors Helena reviews in 2.3 (where a one-line `FOURTH LORD.` also speaks; editors dispute whether the war-lords and court-bachelors are the same men); the 3.2 messengers `FIRST/SECOND GENTLEMAN.` are a DISTINCT prefix from the Act-5 gentle-astringer `GENTLEMAN.`; `ALL.`/`BOTH.` are unison ensembles; this Helena ≠ Midsummer's Helena, this Mariana ≠ Measure's Mariana; Lavatch the Clown = `CLOWN.`, Rinaldo = `STEWARD.`. The title-case phantoms `Excellently.` (First Lord's one-word reply 4.3) and `France.` (a wrap-word) plus the read-aloud letter signatures (`BERTRAM.'` 3.2, `PAROLLES.` 4.3) correctly stayed dialogue. The bed-trick (Helena-for-Diana), the virginity/marriage-market material, and Bertram's coerced marriage + grudging final acceptance are annotated clinically per docs/CONTENT_STANDARDS.md, framed via `the-bed-trick`/`chastity-and-the-double-standard` and presenting the critical problem without endorsement.
- **Troilus and Cressida** — 5 acts, 24 scenes (Act 5 = 10 scenes; editions vary), 3396 TLN'd lines; **345 annotations** (311 at build + 34 rescued from the review queue; 10 dropped, 2 anchor-unresolved of 323 candidates), wired to **20 reference cards** via 131 links — the **Troilus delta**: 3 new historical (`the-matter-of-troy` [the layered Homer-Chapman 1598 / Chaucer / Caxton 1474 / Lydgate sources + the armed Prologue, cited 15×], `cressida-and-the-false-woman` [the content-care anchor — Chaucer + Henryson's *Testament of Cresseid* 1532; the 'false Cressid' tradition, 16×], `pandarus-and-the-pander` [his name → the common noun 'pander', 9×]) + 3 new classical (`the-greek-and-trojan-heroes` [Homer's Iliad + the play's debunking of them, 26×], `achilles-and-patroclus` [the wrath/withdrawal + the ambush-killing of Hector; Iliad 1/9/16-18, 22×], `the-judgement-of-paris` [the war's cause; Ovid Heroides 16 / Lucian, 5×]) + reuse (`the-great-chain-of-being` 6 [Ulysses's 1.3 'degree' speech — reused with a Troilus-specific annotation, no separate degree card], `chastity-and-the-double-standard` 12, `helen-of-troy` 5, `cupid-arrows` 3, `trial-by-combat`/`fall-of-troy` 2 [the Hector-Ajax combat], `hecuba`/`hyperion`/`vulcan-and-the-cyclops`/`fortunes-wheel`/`diana-goddess`/`colossus`/`the-cuckolds-horns`/`niobe` 1 each) — `_review_queue/` cleared (34 rescued, 0 cut), **1079-entry glossary**. The densest, most classical play (difficulty 5, `common_in_curriculum` false). Heavy on `archaic_vocab` (109), `rhetorical_device` (55), `classical_allusion` (53), `historical_topical` (49), `wordplay` (28), `cultural_context` (28), `bawdy_pun` (15), `syntax_grammar` (4), `parallel_passage` (1), `biblical_allusion` (1); confidence high 322 / medium 20 / uncertain 3. Sources: Homer (Chapman 1598) + Chaucer's *Troilus and Criseyde* + Caxton's *Recuyell of the Historyes of Troye*. **TWO parser fixes** (1 slug-gated, 1 general; both byte-identical for all 32 shipped plays by the original-vs-patched re-ingest hash diff): (a) the **armed bare-`PROLOGUE`** — Troilus's Prologue header is the bare word 'PROLOGUE' (no 'THE') and the play has no `CHORUS.` speaker, so a slug-gated `isFramingHeader` branch + a slug-gated broadening of the prologue-speaker-attribution to `BARE_PROLOGUE_HEADER` + a slug-gated skip of the redundant `TROILUS AND CRESSIDA` title line this edition prints between the header and the verse make the Prologue read at TLN 1 (attributed to a `prologue` char aliased `PROLOGUE.`); **gated on the SLUG, not on `hasPrologueSpeaker`, because A Midsummer Night's Dream also defines a `PROLOGUE.` alias** (Quince's play-within), which an alias gate would catch in the content-start scan; (b) a **GENERAL repeated-`ACT n.`-header skip** — this edition reprints the act header before every scene, so the parser first produced 24 phantom acts; skipping an act header whose number equals the already-open act fixes it (byte-identical because no other shipped play repeats an act number). Speaker handling, pure `characters.json` (31 roles) + two OCR-typo aliases: `ANDARUS.` (1.2, a dropped-P `PANDARUS.`) → Pandarus and `AGAME` (1.3, a truncated no-period `AGAMEMNON.`) → Agamemnon (a no-period alias works via `looksLikeSpeaker`'s `aliasToChar.has`; without it Agamemnon's reply mis-attributed to Aeneas) — both caught by `_verify-ingest.mjs`, not extract-speakers; `BOY.` (Troilus's page), `MYRMIDON.` (5.7), `SOLDIERS.` (5.9 unison) added; `SERVANT.` shared (Pandarus's + Diomedes's); the 3.1 Pandarus song 'Love, love, nothing but love' is properly prefixed `PANDARUS.` (no song machinery). Eight title-case phantoms (`Amen.`, `Not I.`, `Troilus.`, `He.`, `Achilles.`, `From Troy.`, `Thersites.`, `From Troy and Troilus.`) correctly stayed dialogue. The misogynistic framing of Cressida as the 'false woman' (her transfer to Diomedes; Troilus's 'Let all untruths stand by thy stained name'), Thersites's scurrility (Patroclus 'masculine whore'), and the dishonourable killing of an unarmed Hector are annotated clinically per docs/CONTENT_STANDARDS.md, framed via `cressida-and-the-false-woman`/`achilles-and-patroclus` and presenting the critical problem without endorsement.
- **Othello** — 5 acts, 15 scenes, 3465 TLN'd lines; **348 annotations** (326 at build + 22 rescued from the review queue; 7 dropped, 1 anchor-unresolved of 355 candidates), wired to **13 reference cards** via 60 links — the **Othello delta**: 3 new historical (`the-handkerchief` [the embroidered 'spotted-with-strawberries' napkin, Othello's first gift and the false 'ocular proof'; the prop comes from Cinthio's source-tale, cited 6×], `cyprus-and-the-ottoman-threat` [the Venetian-Turkish war over Cyprus; the storm-wrecked fleet, the 1570-71 fall of Cyprus and the Lepanto backdrop, 9×], `the-green-eyed-monster` [Iago's coinage; jealousy as a humoral disease, 2×]) + 1 new classical (`the-pontic-sea` [3.3 'Like to the Pontic sea, / Whose icy current...'; Pliny's Natural History via Holland 1601, 1×]) + heavy reuse of the forward-seeded content-care anchor and the bank (`the-moor-on-the-english-stage` 21 [Othello as 'the Moor' — the racism framed, not endorsed; forward-seeded from Titus's Aaron and Merchant's Morocco], `the-stage-machiavel` 14 [Iago the motiveless manipulator], `falconry-and-the-haggard` 1 [3.3 'If I do prove her haggard'], `the-pontic-sea`/`prometheus`/`cupid-arrows`/`diana-goddess`/`the-cuckolds-horns` 1 each) — `_review_queue/` cleared (22 rescued, 0 cut), **1045-entry glossary**. The much-taught tragedy (difficulty 4, `common_in_curriculum` true). Heavy on `archaic_vocab` (160), `historical_topical` (76), `rhetorical_device` (40), `cultural_context` (27), `bawdy_pun` (15), `classical_allusion` (13), `biblical_allusion` (8); confidence high 304 / medium 37 / uncertain 7. Source: Giraldi Cinthio, Gli Hecatommithi 3.7 (1565). Standard 1623-Folio structure, **NO parser change**. Speaker handling, pure `characters.json` (27 roles): the unison cue `DUKE and SENATORS.` (1.3) is an exact-alias ensemble; the numbered Cyprus gentlemen/senators are distinct minor roles; the Willow Song (4.3) and Iago's drinking songs (2.3) are embedded in their speakers' speeches (no song machinery). One post-ingest text.json cleanup: the source ends '[Exeunt.]' + blank lines + 'THE END' with no trailing newline, which the parser took as Lodovico's final spoken line — removed (tln_count 3466→3465). The racism (Iago/Brabantio/Roderigo's epithets; Othello as 'the Moor') framed via `the-moor-on-the-english-stage`, and the murder of Desdemona (intimate-partner violence), are annotated clinically per docs/CONTENT_STANDARDS.md; one annotator-invented type `dramatic_irony` was re-typed `cultural_context` post-merge.
- **The Merry Wives of Windsor** — 5 acts, 23 scenes, 2270 TLN'd lines; **246 annotations** (226 at build + 20 rescued from the review queue; 8 dropped, 5 anchor-unresolved of 254 candidates), wired to **9 reference cards** via 37 links — the **Merry Wives delta**: 2 new historical (`herne-the-hunter` [the horned ghost-keeper of Windsor Forest at Herne's Oak, 4.4/5.5; the play is the earliest record, cited 7×], `the-latin-lesson` [4.1 William Page drilled in Lily's Grammar by Sir Hugh Evans + Mistress Quickly's bawdy mishearings, 4×]) + reuse (`the-cuckolds-horns` 15 [the horn/cuckold jokes climaxing in Falstaff's buck's-head at Herne's Oak — forward-seeded from Much Ado], `the-order-of-the-garter` 4 [the 5.5 fairy blessing of the Garter knights — forward-seeded from Henry VI Part 1], `robin-goodfellow` 3 [the children-as-fairies of 5.5], `oldcastle-and-falstaff`/`robin-hood-and-the-greenwood`/`the-prodigal-son` 1 each) — `_review_queue/` cleared (20 rescued, 0 cut), **1021-entry glossary**. Shakespeare's one comedy of contemporary English middle-class town life (difficulty 3, `common_in_curriculum` false). Heavy on `archaic_vocab` (122), `historical_topical` (60), `cultural_context` (22), `wordplay` (12), `bawdy_pun` (10), `classical_allusion` (7), `biblical_allusion` (5); confidence high 214 / medium 30 / uncertain 2. Source: largely Shakespeare's own (the Falstaff frame). **NO parser change**. Speaker handling, pure `characters.json` (25 roles): the WELSH dialect of Sir Hugh Evans (`EVANS.`) and the FRENCH dialect of Doctor Caius (`CAIUS.`) are handled via the annotate/glossary **`guidance` arg** (gloss/translate the sense where dialect blocks meaning; exclude garbled dialect spellings — 'fery', 'by gar', 'vat' — from the glossary), the Henry-V French-passages pattern (memory `french-passages-annotation-policy`); the 5.5 fairy `SONG.` routes to a `fairies` ensemble (alias `SONG.`); Ford disguises as the stranger 'Brook' (a dialogue name — his prefix stays `FORD.`); the 313-char 5.5 masque stage direction is a legitimate long SD, not a runaway. Light content (comic seduction-attempts, dialect humour).
- **Timon of Athens** — 5 acts, 17 scenes, 2412 TLN'd lines; **253 annotations** (221 at build + 32 rescued from the review queue; 13 dropped, 4 anchor-unresolved of 266 candidates), wired to **9 reference cards** via 53 links — the **Timon delta**: 2 new classical (`timon-the-misanthrope` [Lucian's dialogue 'Timon, or the Misanthrope' + the classical man-hater tradition + Plutarch's Life of Antony; the gold-digging and the flatterers' return, cited 18×], `alcibiades-and-athens` [Plutarch's Life of Alcibiades — the banished general who marches on his own city, the public mirror of Timon's private rage, 7×]) + 1 new historical (`the-banquet-of-stones` [3.6 the mock-feast of warm water and stones; the inversion of hospitality, 3×]) + reuse (`de-casibus-tragedy` 10 [Timon's fall from height], `fortunes-wheel` 6 [the Poet's 1.1 fable of Fortune on her hill], `plutarchs-lives` 5, `hyperion` 2, `the-great-chain-of-being`/`diana-goddess` 1 each) — `_review_queue/` cleared (32 rescued, 0 cut), **919-entry glossary**. A bleak tragic satire co-written with Thomas Middleton, college-level (difficulty 5, `common_in_curriculum` false). Heavy on `archaic_vocab` (94), `rhetorical_device` (61), `cultural_context` (29), `historical_topical` (21), `classical_allusion` (18), `wordplay` (17); confidence high 233 / medium 19 / uncertain 1. Sources: Plutarch's Lives of Antony and Alcibiades (North 1579) + Lucian's Timon + an anonymous academic Timon (c. 1602). **NO parser change** — its sprawling minor cast is pure `characters.json` (61 roles): the apostrophe-label creditors' servants (`LUCIUS' SERVANT.`, `VARRO'S SERVANT.`, `ISIDORE'S SERVANT.`, `FIRST`/`SECOND VARRO'S SERVANT.`), the unison cues (`PHRYNIA AND TIMANDRA.`, `BOTH VARRO'S SERVANTS.`, `SOME SPEAK.`/`SOME OTHER.` [the 3.6 mock-banquet crowd], `ALL LORDS.`/`ALL LADIES.`/`ALL SERVANTS.`, `BANDITTI.`, `BOTH.`), and the masque speakers (`CUPID.`, `FIRST LADY.`) are ALL handled as **exact aliases** — the `aliasToChar.has` short-circuit in `looksLikeSpeaker`/`canonical` bypasses the 18-char length cap and the undotted-unison gate (the Comedy-of-Errors / Merchant precedent), so no parser change was needed; distinct roles keep `LUCILIUS` (Timon's servant) ≠ `LUCIUS` (a flattering lord) ≠ `LUCULLUS`, and `TITUS` is a creditor's servant (not the Roman of Titus Andronicus). One post-ingest text.json reassignment: the 5.4 epitaph and Alcibiades's closing speech (17 lines) were mis-attributed to the `Soldier` because the source marks the speaker change with a stage direction `[ALCIBIADES reads the Epitaph.]` and no `ALCIBIADES.` prefix, leaving curSpeaker = Soldier; reassigned Soldier→Alcibiades (the Richard II `DUCHESS` / Coriolanus pattern). Timon's misanthropy and the venereal curses on Phrynia and Timandra (4.3) are annotated clinically per docs/CONTENT_STANDARDS.md (misogyny flagged, not endorsed).

---

## Reference-card ledger

**210 total** (Session 16 added 9 — Othello: 3 historical (`the-handkerchief` [the spotted-strawberry napkin / the false 'ocular proof'; the prop from Cinthio's source-tale, 6×], `cyprus-and-the-ottoman-threat` [the Venetian-Turkish war over Cyprus; the storm-wrecked fleet + the Lepanto 1571 backdrop, 9×], `the-green-eyed-monster` [Iago's coinage; jealousy as a humoral disease, 2×]) + 1 classical (`the-pontic-sea` [3.3 'Like to the Pontic sea'; Pliny's Natural History via Holland 1601, 1×]); The Merry Wives of Windsor: 2 historical (`herne-the-hunter` [the horned ghost-keeper at Herne's Oak, 4.4/5.5; the play is the earliest record, 7×], `the-latin-lesson` [4.1 William's Lily's-Grammar lesson + Quickly's bawdy mishearings, 4×]); Timon of Athens: 2 classical (`timon-the-misanthrope` [Lucian's dialogue + the classical man-hater tradition + Plutarch's Life of Antony, 18×], `alcibiades-and-athens` [Plutarch's Life of Alcibiades — the banished general who marches on his city, 7×]) + 1 historical (`the-banquet-of-stones` [3.6 the mock-feast of warm water and stones, 3×]) — all 9 used; the targeted-delta reuse thesis held a **16th time** (Othello reused the forward-seeded content-care anchor `the-moor-on-the-english-stage` 21 + `the-stage-machiavel` 14 [Iago] + `falconry-and-the-haggard`/`prometheus`/`cupid-arrows`/`diana-goddess`; Merry Wives reused `the-cuckolds-horns` 15 [forward-seeded from Much Ado] + `the-order-of-the-garter` 4 [forward-seeded from Henry VI Part 1] + `robin-goodfellow` 3; Timon reused `de-casibus-tragedy` 10 + `fortunes-wheel` 6 + `plutarchs-lives` 5 + `hyperion`/`the-great-chain-of-being`/`diana-goddess`). **No new citation-allowlist author was needed** — the new and rescued classical allusions draw on already-allowlisted ancient authors (Pliny, Lucian, Plutarch) plus authors `citation-check` already accepts (Galen, Apollodorus, Aulus Gellius, Boethius). With these three, **the reference-card bank now covers all 37 shipped plays and the Phase-A (drama) build is complete**. Session 15 added 9 — All's Well That Ends Well: 2 historical (`giletta-of-narbonne` [the Boccaccio Decameron 3.9 / Painter 1566 source + the 'clever wench / impossible tasks' folktale, 16×], `the-kings-fistula-and-the-healer` [Helena's cure; empirics vs the College of Physicians + the wonder-cure, 9×]) + 1 classical (`the-miles-gloriosus` [Parolles the braggart-coward; Plautus's Miles Gloriosus, 21×]); Troilus and Cressida: 3 historical (`the-matter-of-troy` [the layered Homer-Chapman 1598 / Chaucer / Caxton 1474 / Lydgate sources + the armed Prologue, 15×], `cressida-and-the-false-woman` [the content-care anchor — Chaucer + Henryson's Testament of Cresseid 1532, 16×], `pandarus-and-the-pander` [his name → the common noun 'pander', 9×]) + 3 classical (`the-greek-and-trojan-heroes` [Homer's Iliad + the play's debunking of them, 26×], `achilles-and-patroclus` [the wrath/withdrawal + the ambush-killing of Hector, 22×], `the-judgement-of-paris` [the war's cause; Ovid Heroides 16 / Lucian, 5×]) — all 9 used; the targeted-delta reuse thesis held a **15th time**, both problem plays reusing forward-seeded and bank cards (All's Well: `the-bed-trick` 13 [forward-seeded from Measure, Helena-for-Diana], `chastity-and-the-double-standard` 11 [the 1.1 virginity debate], `palmers-and-pilgrims` 5 [Helena's pilgrimage to Saint Jaques], `fortunes-wheel`/`diana-goddess` 3 each, `helen-of-troy`/`juno-goddess`/`hercules` 1; Troilus: `the-great-chain-of-being` 6 [Ulysses's 'degree' speech — REUSED with a Troilus annotation, no separate degree card], `chastity-and-the-double-standard` 12, `helen-of-troy` 5, `cupid-arrows` 3, `trial-by-combat`/`fall-of-troy` 2, plus the Troy/classical bank `hecuba`/`hyperion`/`niobe`/`colossus`/`vulcan-and-the-cyclops`/`fortunes-wheel`/`diana-goddess`/`the-cuckolds-horns` 1 each). `the-jew-on-the-english-stage`/`the-moor-on-the-english-stage`/`the-quality-of-mercy` remain forward-seeded for Othello (mop-up). **One citation-allowlist add this session**: `Publilius Syrus` (1st-c. BCE Latin maxim-writer) — the rescued Troilus 3.2 'to be wise and love exceeds man's might' cites his genuine maxim 'amare et sapere vix deo conceditur'; an honest, accurate ancient source. Session 14 added 10 — The Merchant of Venice: 4 historical (`the-jew-on-the-english-stage` [the content-care anchor — the usurer-'Jew' stereotype + Marlowe's Barabas + the 1290 expulsion + the 1594 Lopez affair; PARALLEL to `the-moor-on-the-english-stage`, cited 22×], `usury-and-the-bond` [Christian-vs-Jewish lending; Aristotle's 'barren metal' + the 1571 statute, 7×], `the-three-caskets` [gold/silver/lead; the Gesta Romanorum / Robinson 1577, 7×], `the-quality-of-mercy` [Portia's 4.1 plea; mercy/justice + equity-vs-law, 3×]) + 1 biblical (`jacob-and-laban` [Genesis 30:25-43; Shylock's 1.3 defence of interest, 2×]); Measure for Measure: 4 historical (`the-disguised-ruler` [the Duke-as-friar play; Marston/Middleton + James I's *Basilikon Doron*, 13×], `the-precise-puritan` [Angelo 'precise'; cf. Malvolio, 14×], `the-bawdy-houses-of-vienna` [the Bankside stews / 1.2 suburb-brothel proclamation, 14×], `the-bed-trick` [Mariana's substitution; Cinthio/Whetstone; cf. All's Well, 11×]) + 1 biblical (`measure-for-measure-sermon-on-the-mount` [the title — Matthew 7:1-2 'with what measure ye mete'; the 5.1 'measure still for measure', 13×]) — all 10 used; the targeted-delta reuse thesis held a **14th time**, both problem plays reusing forward-seeded and bank cards (Merchant: `the-renaissance-friendship-ideal` 9 [forward-seeded from Two Gentlemen, for Antonio/Bassanio], `the-moor-on-the-english-stage` 5 [the Prince of Morocco], `hercules` 3 ['young Alcides'], `pythagoras-and-the-transmigration-of-souls` 1 [Gratiano's 'currish spirit'], `the-prodigal-son`/`diana-goddess` 2 each, `cupid-arrows`/`fortunes-wheel`/`pyramus-thisbe`/`dido-queen-of-carthage`/`erebus`/`cato-the-younger` 1 each; Measure: `shrift-and-confession` 12 [forward-seeded, the Duke-friar's confessions], `chastity-and-the-double-standard` 11 [Isabella], `the-constable-and-the-watch` 4 [Elbow], `pompey-the-great` 2, `the-vice`/`jupiter-king-of-gods`/`the-roman-triumph` 1 each). `the-jew-on-the-english-stage` (parallel to `the-moor`) and `the-quality-of-mercy` are forward-reusable (Othello; the mercy/justice theme); `the-bed-trick`/`the-disguised-ruler` for All's Well That Ends Well; `the-precise-puritan` ties to the existing `puritanism-and-revelry` (Malvolio). No new citation-allowlist authors were needed — the few classical allusions reuse the existing bank (Hercules, Pythagoras via Ovid Met. 15). Session 13 added 9 — As You Like It: 4 historical (`the-pastoral-and-the-forest-of-arden` [the pastoral 'green world' + Lodge's *Rosalynde*; the court-vs-country debate, cited 5×], `melancholy-and-the-malcontent` [Jaques the Galenic-melancholy malcontent; Bright 1586, 7×], `robin-hood-and-the-greenwood` [Duke Senior's band 'like the old Robin Hood', 2× — and immediately reused 5× for Two Gentlemen's outlaws], `the-seven-ages-of-man` [Jaques's 2.7 'All the world's a stage' + the theatrum mundi, 2×]) + 3 classical (`ganymede` [Rosalind's disguise-name; Jove's cupbearer + the boy-actor/same-sex resonance, 6×], `the-golden-age` [Duke Senior's 'golden world'; Ovid Met. 1, 4×], `hymen-god-of-marriage` [the 5.4 wedding masque, 1×]); Two Gentlemen of Verona: 1 classical (`proteus-the-shape-changer` [the inconstant gentleman's name; Homer/Virgil's shape-shifting sea-god, 7×]) + 1 historical (`the-renaissance-friendship-ideal` [the amicitia / Titus-and-Gisippus ideal behind Valentine's 5.4 offer; Cicero/Elyot 1531, 2×]) — all 9 used; the targeted-delta reuse thesis held a **13th time**, both festive comedies reusing the comedy/love banks (`petrarch-and-the-sonnet` 7+15 [Orlando's tree-verses; the love-letters], `cupid-arrows` 4+4, `hero-and-leander` 2+2 [Rosalind's 'Leander'/Phebe's Marlowe quote; the 1.1 Hellespont], `diana-goddess` 3+1, `fortunes-wheel` 1+1, `phaethon` (Two Gentlemen 3.1), `the-cuckolds-horns` 4 [As You Like It's horn song + Touchstone], `ovids-metamorphoses`/`hercules`/`the-roman-triumph`/`juno-goddess` (As You Like It), `elysium`/`philomela`/`shrift-and-confession` (Two Gentlemen)). `robin-hood-and-the-greenwood` (authored for As You Like It) was reused the same session for Two Gentlemen's greenwood outlaws; `ganymede`/`hymen-god-of-marriage`/`the-pastoral-and-the-forest-of-arden`/`the-renaissance-friendship-ideal` are forward-reusable (the wedding masques, the green-world comedies, the friend-vs-love plays — The Merchant of Venice, The Two Noble Kinsmen). Session 12 added 11 — Much Ado: 4 historical (`the-cuckolds-horns` [the horn/cuckold jokes; the Actaeon root, cited 12×], `nothing-and-noting` [the title's triple pun 'nothing'≈'noting' + bawdy 'no-thing', 16×], `chastity-and-the-double-standard` [Hero's slander; via Vives 1529, 11×], `the-constable-and-the-watch` [Dogberry + the Elizabethan watch; via Lambarde 1582, 17×]) + 1 classical (`hero-and-leander` [Benedick's 5.2 'Leander the good swimmer'; Marlowe 1598 / Ovid / Musaeus]); Twelfth Night: 3 historical (`twelfth-night-and-the-lord-of-misrule` [the Epiphany/Saturnalia inversion; via Stubbes 1583, 6×], `puritanism-and-revelry` [Malvolio 'a kind of Puritan', 6×], `illyria` [the Adriatic setting]) + 3 classical (`arion-and-the-dolphin` [1.2, via Herodotus], `elysium` [1.2, the Illyria pun], `pythagoras-and-the-transmigration-of-souls` [4.2 Sir Topas, via Ovid Met. 15]) — all 11 used; the targeted-delta reuse thesis held a **12th time**, both festive comedies reusing the comedy/love banks (`cupid-arrows`, `diana-goddess`, `petrarch-and-the-sonnet`, `the-stage-machiavel` for Don John, `falconry-and-the-haggard`, `lethe`, `tarquin-and-lucrece` for Malvolio's Lucrece seal). `hero-and-leander`/`pythagoras-and-the-transmigration-of-souls` are forward-reusable (As You Like It / the poems; The Merchant of Venice); `the-cuckolds-horns`/`chastity-and-the-double-standard` for Othello and The Winter's Tale; `the-constable-and-the-watch`/`twelfth-night-and-the-lord-of-misrule`/`puritanism-and-revelry` for Measure for Measure and the festive comedies. `Musaeus` and `Heliodorus` joined the classical-citation allowlist (the Hero-and-Leander and 'Egyptian thief' allusions). Session 11 added 9 — Macbeth: 4 historical (`the-weird-sisters-and-witchcraft` ['wyrd' = fate; James I's *Daemonologie* + the 1604 witch-statute, cited 14×], `equivocation-and-the-gunpowder-plot` [the Porter's 'equivocator' 2.3 + the 1606 Garnet trial, 8×], `the-kings-evil` [the 4.3 royal touch for scrofula, 4×], `banquo-and-the-stuart-line` [the 4.1 show of eight kings; James's claimed descent via Fleance, 4×]); King Lear: 5 historical (`harsnetts-declaration-and-the-devils` [Poor Tom's fiends from Harsnett 1603, 9×], `king-leir-and-legendary-britain` [Geoffrey of Monmouth / Holinshed + Shakespeare's changed tragic ending, 7×], `astrology-and-the-eclipses` [Gloucester's 1.2 eclipses vs Edmund's mockery, 5×], `dover-and-the-cliff` [the 4.6 staged fall, 4×], `the-division-of-the-kingdom` [the 1.1 partition + the Jacobean Union, 2×]) — all 9 used; the targeted-delta reuse thesis held an **11th time**, both Holinshed-sourced tragedies reusing the history "spine" directly (`holinshed-chronicles` 17, `the-great-chain-of-being` 8+15, `the-kings-two-bodies` 2+9, `de-casibus-tragedy` 3+4, `divine-right-of-kings` 5+1, `fortunes-wheel` 2+7, `trial-by-combat` 3 — Edgar vs Edmund) + `triple-hecate` 5+2 (Hecate, the pagan gods) + `the-stage-machiavel` 7 (Edmund). `harsnetts-declaration-and-the-devils` is forward-reusable wherever feigned possession/exorcism recurs; `king-leir-and-legendary-britain` and `the-division-of-the-kingdom` anchor the legendary-British matter. Session 10 added 7 — Antony and Cleopatra: 5 historical (`the-second-triumvirate` [the three world-sharers, cited 32×], `the-ptolemies-and-egypt` [Egypt vs Rome, 18×], `the-battle-of-actium` [17×], `the-asp` [Cleopatra's death], `augustus-and-the-pax-romana` [the 4.6 'universal peace']) + 2 classical (`cleopatra-as-isis` [the goddess-queen, 7×], `the-barge-on-the-cydnus` [Enobarbus's 2.2 set-piece, 5×]) — all 7 used; the targeted-delta reuse thesis held a 10th time, the Julius Caesar/Coriolanus Roman/Plutarch bank reused heavily (`plutarchs-lives` 14, `the-antique-roman` 8 — the 'high Roman fashion' suicides, `pompey-the-great` 7, `the-roman-triumph` 6, `roman-augury` 5, `hercules` 3, plus the predicted forward-reuse of `dido-queen-of-carthage`). `the-second-triumvirate` and `augustus-and-the-pax-romana` are forward-reusable across the Roman material; `the-asp`/`cleopatra-as-isis`/`the-barge-on-the-cydnus` carry into the poems' Cleopatra/Egypt imagery. Session 9 added 8 — Coriolanus: historical `the-fable-of-the-belly`, `the-tribunes-of-the-people`, `the-roman-consulship`, `the-volscians-and-corioli`; Titus Andronicus: classical `ovids-metamorphoses` (the Ovid-bank keystone — the book carried on stage in 4.1; forward-reusable for the Phase-B poems and corpus-wide), historical `seneca-and-revenge-tragedy`, `the-moor-on-the-english-stage`, `virginius-and-virginia` — all 8 used; the targeted-delta reuse thesis held a 9th time, with Julius Caesar's Roman/Plutarch bank reused into Coriolanus (`plutarchs-lives` 32, `the-roman-triumph`, `tarquin-and-the-first-brutus`, `roman-republic-and-the-name-king`, `de-casibus-tragedy`) and the Ovid/classical bank into Titus (`philomela` 9, `tarquin-and-lucrece` 4, `fall-of-troy`, `the-vice`, `dido-queen-of-carthage`, `astraea`, `hecuba`). `ovids-metamorphoses` and `seneca-and-revenge-tragedy` are forward-reusable for the narrative poems (Venus and Adonis, Rape of Lucrece) and the revenge tragedies (Hamlet); `the-moor-on-the-english-stage` for Othello and the Merchant of Venice's Prince of Morocco. Session 8 added 12 — Shrew: classical `venus-and-adonis`, `daphne-and-apollo`; historical `falconry-and-the-haggard`, `patient-griselda`, `the-sleeper-awakened`. Love's Labour's Lost: classical `prometheus`; historical `the-nine-worthies`, `the-masque-of-muscovites`; biblical `samson-and-delilah`. Comedy of Errors: classical `plautus-and-roman-comedy`, `circe`; biblical `st-paul-and-ephesus` — 11 of 12 used (`the-masque-of-muscovites` authored but not yet linked, like `giulio-romano`); the targeted-delta reuse thesis held an 8th time, with `cupid-arrows` (8), `hercules` (6), `petrarch-and-the-sonnet`, `hesperides`, `tarquin-and-lucrece`, `gower-and-confessio-amantis`, `fortunes-wheel` reused as predicted. `falconry-and-the-haggard` is forward-reusable for Othello/Twelfth Night; `prometheus` for Othello; `plautus-and-roman-comedy`/`st-paul-and-ephesus` for the comedies. Session 7 added 7 — classical: `jupiter-king-of-gods`, `tarquin-and-lucrece` (Cymbeline), `apollo-and-the-delphic-oracle`, `flora-goddess-of-flowers` (The Winter's Tale); historical: `ancient-britain-and-the-roman-tribute`, `milford-haven` (Cymbeline), `giulio-romano` (The Winter's Tale) — 6 of 7 used (`giulio-romano` authored but not yet linked); the targeted-delta reuse thesis held a 7th time, with `ceres-and-proserpina`/`lucina`/`neptune`/`diana` reused into the romances as predicted. Session 6 added 13 — classical: `juno-goddess`, `ceres-and-proserpina`, `iris-rainbow`, `dido-queen-of-carthage`, `the-harpy`, `neptune-god-of-the-sea`, `lucina-goddess-of-childbirth`, `hesperides`; historical: `the-bermuda-wreck`, `montaigne-of-the-cannibals`, `setebos-patagonian-god` (The Tempest), `gower-and-confessio-amantis`, `apollonius-of-tyre` (Pericles) — the romance set, all used; the targeted-delta reuse thesis held a 6th time, and several new cards are forward-reusable (`neptune` → Winter's Tale/Hamlet, `ceres-and-proserpina` → Winter's Tale, `lucina` → Cymbeline/Titus, `dido` → Antony and Cleopatra). Session 5 added 12 — historical: `the-angevin-succession`, `the-papal-interdict`, `richard-the-lionheart`, `the-troublesome-raigne` (King John); `cardinal-wolsey` (cited 43×), `the-kings-great-matter` (38×), `the-break-with-rome`, `foxes-acts-and-monuments`, `cranmers-prophecy-of-elizabeth`, `field-of-the-cloth-of-gold` (Henry VIII); biblical: `the-fall-of-the-angels` (Lucifer's pride — Wolsey's 'by that sin fell the angels'; reusable corpus-wide for ambition, e.g. Macbeth), `queen-of-sheba` (the 'Saba' of Cranmer's prophecy)). With King John + Henry VIII shipped, **all ten English history plays now ship and are card-complete**, and all 12 new cards were used (the targeted-delta reuse thesis held a 5th time). With Richard III the **first tetralogy is card-complete**: `wars-of-the-roses` (the cross-tetralogy keystone) was reused 40 (H6.3) + 22 (R3); `edward-iii-and-his-sons` is the genealogical keystone cited by every history. The history "spine" (`edward-iii-and-his-sons`, `the-kings-two-bodies`, `the-great-chain-of-being`, `de-casibus-tragedy`, `trial-by-combat`, `holinshed-chronicles`, `pilate-washing-hands`) plus `divine-right-of-kings`/`the-vice`/`fortunes-wheel`/`cain-and-abel` remain corpus-wide infrastructure; `holinshed-chronicles` carries `related_plays` for all 10 histories + Macbeth. `the-stage-machiavel` and `daedalus-and-icarus` are cross-corpus conceptual cards (reusable for Iago/Edmund/Aaron, and for the Icarus-image of ambition).

---

## Accuracy sweeps

**2026-05-31 accuracy sweep**: verified all **55 reference cards** (tightened 3 citations — `hyperion` Ovid locus, `mandrake-folklore` Pliny book, `prince-of-cats` Nashe quote, the last confirmed *correct* against the 1596 text); audited every `medium`/`uncertain` annotation (fixed 6, incl. Cicero *De Amicitia* §80 and the Geneva 1 Peter 3:7 "woman"); and **cleared all four `_review_queue/`s — 97 items rescued with corrected or generalized (verifiable) citations, 0 cut**. The failures were citation-precision issues, not bad glosses, so the fix was to make citations honest (drop fabricated/wrong loci, generalize to work level, fix Geneva-vs-KJV wording) and trim 4 interpretation-overreaches to their sourced cores. Total annotations **937 → 1034**; all three validators green.

---

## Pipeline & process history

**Pipeline proven on thirty-seven plays** (four pilots + all ten English histories + the four romances + three framing/pageant comedies + the three Roman tragedies + the two Holinshed tragedies + the four festive comedies (Much Ado About Nothing + Twelfth Night + As You Like It + The Two Gentlemen of Verona) + the four problem plays (The Merchant of Venice + Measure for Measure + All's Well That Ends Well + Troilus and Cressida) + the mop-up trio (Othello + The Merry Wives of Windsor + Timon of Athens) — the Henriad *Richard II–Henry V*, the complete first tetralogy *Henry VI Parts 1-3 + Richard III*, the two outliers *King John + Henry VIII*, and *The Taming of the Shrew + Love's Labour's Lost + The Comedy of Errors*): ingest → Sonnet annotator subagents → deterministic anchor resolution (`resolve-anchors.ts`, shared matcher in `lib/text.ts`) → adversarial fact-check (source + interpretation Sonnet judges + deterministic anchor judge, merged by `apply-verdicts.ts`) → validators → Astro reader. Glossary uses the same generate→fact-check pattern (`merge-glossary.ts`, `apply-glossary-verdicts.ts`). **`reanchor.ts`** re-resolves shipped annotations after a re-ingest so TLN shifts don't break anchors. All 3 validators pass; the site builds. Julius Caesar was run via the **Workflow** tool (one annotator subagent per scene → source+interp judges per scene), then the deterministic merge steps run serially from inside `scripts/` (must use `node --import tsx …` from `scripts/`; `node`'s `execFileSync` can't spawn the `.bin/tsx.cmd` shim on Windows — EINVAL). Romeo and Juliet ran the same way (24 annotators + 48 judges, then a 5-act glossary fan-out). The `workflows/annotate.mjs` and `workflows/glossary.mjs` templates were hardened to accept `args` delivered as a JSON **string** as well as an object (`const input = typeof args === 'string' ? JSON.parse(args) : args`) — without it the Workflow tool can throw "Workflow args must be…" before any agent runs. **Session 2** (H4.2 + Henry V) added an optional `guidance` arg to both Workflow templates (appended to every annotator/lexicographer prompt) — used for Henry V's French-translation instructions and the French-glossary exclusion; it is the reusable hook for H6.1's Joan. **Concurrency lesson:** running the two plays' heavy annotate Workflows *concurrently* (~126 agents) saturated the machine and ~55-60% of later-scene annotator agents finished WITHOUT writing their candidate files (the workflow still reports all agents "completed"); the candidate files were simply missing (not malformed). Re-running the missing scenes **one workflow at a time** fixed it cleanly. **Rule: at most one heavy annotate Workflow at a time** (a light ~5-agent glossary Workflow alongside is fine). The `_check-candidates` / unmapped-speaker / detail-summary-fix helpers were throwaway diagnostics (recipe in memory `henriad-b-build-notes`). **Session 4** (Henry VI Part 3 + Richard III) re-confirmed the concurrency rule — one heavy annotate + one light glossary per play, sequential across plays — with **0 candidate loss (290/290 and 373/373)**. **Session 5** (King John + Henry VIII) confirmed it a 4th time (259/259 and 319/319, 0 loss) and cleared **all four outstanding `_review_queue/`s in a single 4-agent Opus rescue Workflow — 83 items rescued, 0 cut** (John 20 + Henry VIII 21 + the deferred Henry VI Part 3 20 + Richard III 22) via `scripts/pipeline/_merge-rescued.mjs`; one rescued classical citation (the Olympian games, correctly re-sourced to Pindar/Pausanias) prompted adding those two ancient authors to the citation allowlist. **Session 6** (The Tempest + Pericles — the first romances) confirmed the concurrency rule a 5th time (**0 candidate loss**: Tempest 9/9 scenes, Pericles 22/22) and for the first time *overlapped* a heavy annotate with a light glossary within the rule (Pericles annotate ~66 agents + glossary 5 agents, peak ~21 concurrent — fine). Both new queues were cleared by a **2-agent Opus rescue Workflow** (`workflows/_rescue-queues.mjs`, one reviewer per play — **41 rescued, 0 cut**); the rescue again caught fabricated citations (a non-existent Twelfth Night line, a phantom Hakluyt note) and corrected Ovid/Virgil loci against the Latin texts. Also folded in the Phase-C **Titus source-path alias** (`scripts/lib/paths.ts` `MODERN_SOURCE_ALIAS`: `titus_andronicus` → `tragedy_of_titus_andronicus_gut.txt`), unblocking the eventual Titus build. **Session 7** (Cymbeline + The Winter's Tale) confirmed the concurrency rule a 6th time (**0 candidate loss**: Cymbeline 27/27, Winter's Tale 15/15; each heavy annotate overlapped with its own light glossary, peak ~21 concurrent) and cleared both new queues with the 2-agent Opus rescue Workflow (**49 rescued, 0 cut**) — one rescued Apollo-oracle citation again needed an allowlisted ancient source (Herodotus/Pausanias on Delphi). It also handled the **first defective vendored source**: Cymbeline's modern `_gut.txt` was missing Acts III-IV, repaired by converting the complete public-domain Moby XML to standard `_gut.txt` form (`_moby-to-gut.mjs` → committed `data/sources/cymbeline_gut.txt`, slug-gated `FULL_SOURCE_OVERRIDE` in `paths.ts`; the 16 prior shipped plays re-ingest byte-identical, verified). **Time-as-Chorus (Winter's Tale 4.1) and Jupiter's theophany (Cymbeline 5.4) needed NO parser change** — Time is a plain `time` chorus character in its own scene 4.1, and the deity/ghost speakers are pure `characters.json` aliases. **Session 8** (Taming of the Shrew + Love's Labour's Lost + Comedy of Errors — framing/pageant comedies) confirmed the concurrency rule a 7th time (**0 candidate loss**: Shrew 14/14 scenes, LLL 9/9, Errors 11/11; each heavy annotate overlapped with its own light glossary, sequential across the three plays) and cleared all three new queues with the 3-agent Opus rescue Workflow (**58 rescued, 0 cut**: Shrew 31 + LLL 16 + Errors 11). The session's gated parser additions stayed byte-identical for the 18 prior shipped plays (re-ingest-diff: only the date stamp + the documented R2-DUCHESS/H4.1-GLEND reverts differ). **Session 9** (Coriolanus + Titus Andronicus — the first reuse-clustered Roman tragedies) confirmed the concurrency rule an 8th time (**0 candidate loss**: Coriolanus 29/29 scenes, Titus 14/14; each heavy annotate overlapped its own light glossary, and for the first time a play's light glossary — Titus — overlapped the *other* play's still-running heavy annotate — Coriolanus — still within one-heavy-at-a-time) and cleared both new queues with the 2-agent Opus rescue Workflow (**66 rescued, 0 cut**: Coriolanus 49 + Titus 17). **NO parser change** (both plays are standard structure, as the roadmap predicted for the reuse-clustered remainder); the only build-kit change was teaching `extract-speakers.mjs` the Titus source-path alias (it had not honoured `paths.ts`'s `MODERN_SOURCE_ALIAS`), and the classical-citation allowlist gained `Diodorus Siculus` (Semiramis). Both plays' two cosmetic Titus setting fixes (dangling 'Horns', stray ']') were post-ingest text.json edits. **Session 10** (Antony and Cleopatra — solo, the second Roman-tragedies block) confirmed the concurrency rule a **9th time** (**0 candidate loss**: 42/42 scenes; the single heavy annotate ran ~126 annotator+judge agents — budgeted like a heavy two-play session — overlapped only by the light 5-agent glossary) and cleared the new queue with the 1-agent Opus rescue Workflow (**68 rescued, 0 cut**; ship rate ~78%, 310/396 at the conservative 2-of-3 gate). It required the **first plays-wide parser change since Session 8** — two *general* (non-gated) fixes in `parse-gutenberg.ts`: (a) `toInt` now parses Roman numerals by subtractive notation (`romanToInt`) instead of an I..X lookup table that returned 0 for XI+ — A&C is the first play with an act of more than ten scenes (Act 3 has 13, Act 4 has 15), so the initial ingest silently merged XI-XV into scene X, yielding only 33 of 42 scenes; (b) a pre-pass splits a combined `ACT n. SCENE m.` header line (A&C's Act 4 only, whose other four acts use a separate `ACT n.` line) into its two canonical lines. Because no shipped play has either shape, both are byte-identical for all 23 prior plays — proven not by the usual committed-diff (which conflates the date stamp and the documented R2-DUCHESS/H4.1-CAR/GLEND manual reverts) but by an **original-parser-vs-patched-parser re-ingest hash comparison**: re-ingest all 23 under both parser versions the same day → identical SHA-256 hashes. 18 annotator-emitted `detail: null` fields were stripped from `annotations.json` post-merge to satisfy the schema (a latent gap: the deterministic merge does not yet strip null/empty `detail`). **Session 11** (Macbeth + King Lear — the Holinshed tragedies) confirmed the concurrency rule a **10th and 11th time** but also REFINED it. Macbeth's heavy annotate (26/26 scenes, 0 loss) overlapped only its light 5-agent glossary and was clean; but **Lear's first heavy annotate lost 23 of 26 scenes' candidate files** — *not* because a second heavy annotate ran, but because heavy *local* node pipeline work (Macbeth's `postprocess` + `build-index` over ~83k docs + `audit`) was saturating the machine concurrently. Re-running the 23 missing scenes **with the machine quiet** recovered them cleanly (26/26). **Refined rule: keep heavy local node work (postprocess / build-index / audit) OFF the machine while a heavy annotate Workflow runs — not just other annotate Workflows.** Both new queues were cleared by the 2-agent Opus rescue Workflow (**66 rescued, 0 cut**: Macbeth 25 + Lear 41 — the citation-fix-not-cut policy held an 11th time). **NO parser change** (both plays are standard structure, as the roadmap predicted; Lear's title-case `Scene I.` headers and title-case speaker prefixes parse like Hamlet via the case-insensitive `SCENE_HEADER` regex + complete `characters.json` aliases, verified by a per-character speech-count check). The session landed the **deferred null-`detail` cleanup** flagged in Session 10: `apply-verdicts.ts`'s `clean()` now drops a null/undefined `detail` (a real or empty-string detail is preserved), fixing Macbeth's 52 annotator-emitted `detail: null` fields at the source rather than post-merge — verified byte-identical for every prior play (a corpus scan found 0 prior plays carry a *null* detail; their schema-valid empty-string details are deliberately left untouched). `_merge-rescued.mjs` already strips non-string detail. Ship rates ~84% (Macbeth 208/249) / ~86% (Lear 323/376). **Session 12** (Much Ado About Nothing + Twelfth Night — the festive comedies) confirmed the concurrency rule a **12th and 13th time** (**0 candidate loss**: Much Ado 17/17 scenes, Twelfth Night 18/18; each heavy annotate overlapped only its own light 5-agent glossary, sequential across the two plays, and the refined Session-11 rule was observed — no postprocess/build-index/audit ran while either annotate was in flight). Both new queues were cleared by the 2-agent Opus rescue Workflow (**66 rescued, 0 cut**: Much Ado 35 + Twelfth Night 19 — the citation-fix-not-cut policy held a 12th time). Two **gated, byte-identical-verified** song-attribution parser fixes (Much Ado's `SINGER_SD_SETS_SPEAKER`: `[X sings.]` sets the named singer as speaker so 'Sigh no more' is Balthasar's; Twelfth Night's `TITLE_SONG_CUE`: a title-case `Song.` cue reads as a header so the closing song stays Feste's) plus Sir Andrew's hyphenated `AGUE-CHEEK.` alias (pure `characters.json`); both fixes proven byte-identical for all 26 prior plays by the original-vs-patched re-ingest hash diff (incl. the Tempest's Ariel songs and Macbeth's aliased title-case `Song.`). `Musaeus` (Hero and Leander) and `Heliodorus` (the 'Egyptian thief', Twelfth Night 5.1) joined the citation allowlist; the only audit cleanups were three citations (a contiguity fix for Ecclesiasticus 13:1, the Heliodorus allowlist add, and re-typing the Epistles/Gospels note to `cultural_context`). Ship rates ~82% (Much Ado 196/238) / ~85% (Twelfth Night 212/249). **Session 13** (As You Like It + The Two Gentlemen of Verona — the festive comedies continued) confirmed the concurrency rule a **14th and 15th time** (**0 candidate loss**: As You Like It 22/22 scenes, Two Gentlemen 20/20; each heavy annotate overlapped only its own light 5-agent glossary, sequential across the two plays, with no postprocess/build-index/audit while either annotate was in flight). Both new queues were cleared by the 2-agent Opus rescue Workflow (**43 rescued, 0 cut**: As You Like It 20 + Two Gentlemen 23 — the citation-fix-not-cut policy held a 13th time). THREE **gated, byte-identical-verified** parser fixes: As You Like It widens `SINGER_SD_SETS_SPEAKER` to attribute its `[AMIENS sings.]` song 'Blow, blow, thou winter wind' to Amiens (+ a `song` ensemble for its two bare-`SONG.` songs); The Two Gentlemen of Verona adds `SONG_SD_SETS_SINGER` (its bracketed `[SONG]` serenade 'Who is Silvia?' → a `Musicians` ensemble) and a slug-gated inline/bracketed speaker-label splitter for the 1.1 'nod/ay/noddy' stichomythia (`[SPEED] Ay.` / `PROTEUS. Nod, ay?...`) — all three proven byte-identical for all 28 prior plays by the original-vs-patched re-ingest hash diff (only the date stamp + the documented R2/H4.1/Titus manual reverts differ). The only audit cleanups were a `classical_allusion`→`parallel_passage` re-type (Phebe quoting Marlowe's *Hero and Leander*, not an allowlisted ancient author) and two over-600 summary trims (Two Gentlemen's musical-puns + codpiece notes). Ship rates ~90% (As You Like It 231/256) / ~87% (Two Gentlemen 206/238). **Session 14** (The Merchant of Venice + Measure for Measure — the problem plays) confirmed the concurrency rule a **16th and 17th time** (**0 candidate loss** of annotator files: Merchant 20/20 scenes, Measure 18/18; each heavy annotate overlapped only its own light 5-agent glossary, sequential across the two plays, with no postprocess/build-index/audit while either annotate was in flight). A new wrinkle: a few *judge* files (not annotator candidates) dropped on the first pass — Merchant 2 of 40 (1.1 source, 2.2 interp), Measure 1 of 18 (4.3 source) — transient agent write-failures with the machine otherwise quiet; the **pre-postprocess candidate-completeness check** (per-scene presence of `<a>-<s>.json` + `.source.json` + `.interp.json`) caught them, and re-running just those scenes restored full 3-judge coverage before the 2-of-3 gate ran. Both new queues were cleared by the 2-agent Opus rescue Workflow (**49 rescued, 0 cut**: Merchant 28 + Measure 21 — the citation-fix-not-cut policy held a **14th time**). TWO **gated, byte-identical-verified** parser fixes: Merchant's `SONG_SD_DESC_SETS_SINGER` (the descriptive `[A Song, whilst BASSANIO comments on the caskets to himself.]` cue, which names no singer and would fold 'Tell me where is fancy bred' into Portia, → a `musicians` ensemble alias `SONG.`) and Measure's `UNISON_AND_UNDOTTED` (the 5.1 `ANGELO and ESCALUS.` undotted-first unison, which the dotted-parts test missed and the length cap then mis-read as dialogue, → the joined 'Angelo and Escalus' like JC's Titinius/Messala) — both proven byte-identical for all 30 prior shipped plays by the original-vs-patched re-ingest hash diff (only the date stamp + the documented R2/H4.1/Titus manual reverts differ). Measure's 4.1 Boy-sung song 'Take, O take those lips away' needed **NO** parser change: the Boy is a `boy` character aliased `SONG`/`SONG.`, so its all-caps `SONG` header attributes the unlabeled lyrics via the existing aliased-`SONG`-header speaker path (as As You Like It's bare-`SONG.` ensemble does) — and without it the lyrics would have been *dropped* (curSpeaker is null at scene-open). The only audit cleanups were two honest re-types (`merchant_of_venice-1333` Hercules/Mars 'white liver' note `classical_allusion`→`cultural_context` — it is about humoral physiology, sourced to Bright 1586; `measure_for_measure-1260` 'the deadly seven' `biblical_allusion`→`cultural_context` — the seven-deadly-sins scheme is patristic, NOT a single biblical passage, per its own Gregory/Aquinas source), one biblical citation given a Book:Ch:V locus (`merchant_of_venice-2014` 'A Daniel come to judgment' → the History of Susanna / Daniel 13:45-64), and three over-600 summary trims. **No new citation-allowlist authors needed** (the classical allusions reuse the existing bank). Ship rates ~85% (Merchant 226/266) / ~90% (Measure 269/298). **Session 15** (All's Well That Ends Well + Troilus and Cressida — the problem plays continued) confirmed the concurrency rule an **18th and 19th time** (**0 annotator-candidate loss**: All's Well 23/23 scenes, Troilus 24/24; each heavy annotate overlapped only its own light 5-agent glossary, sequential across the two plays, with no postprocess/build-index/audit/re-ingest-regression in flight). As in Session 14, one *judge* file dropped on the first pass (Troilus 4.5 `.source.json`) — the **pre-postprocess candidate-completeness check** caught it, and re-running just that scene through the annotate Workflow restored full 3-judge coverage before the 2-of-3 gate. Both new queues were cleared by the 2-agent Opus rescue Workflow (**60 rescued, 0 cut**: All's Well 26 + Troilus 34 — the citation-fix-not-cut policy held a **15th time**). **All's Well needed NO parser change** (its 1.3 Clown song is properly prefixed `CLOWN.` + `[Sings.]`, attributed by the standard path). **Troilus needed TWO** (1 slug-gated, 1 general), both proven byte-identical for all 32 shipped plays by the original-vs-patched re-ingest hash diff (only the date stamp + the documented R2/H4.1/Titus manual reverts differ): the **slug-gated armed bare-`PROLOGUE`** handling (a bare 'PROLOGUE' header with no 'THE' and no `CHORUS.` speaker → a slug-gated `isFramingHeader` branch + a slug-gated broadening of the prologue-speaker-attribution to `BARE_PROLOGUE_HEADER` + a slug-gated skip of the redundant `TROILUS AND CRESSIDA` title line; gated on the SLUG not on `hasPrologueSpeaker` because A Midsummer Night's Dream also defines a `PROLOGUE.` alias that an alias-gate would misfire in the content-start scan), and a **GENERAL repeated-`ACT n.`-header skip** (this edition reprints the act header before every scene → the parser first made 24 phantom acts; skip an act header whose number equals the open act). Two OCR-typo aliases (`ANDARUS.`→Pandarus, `AGAME`→Agamemnon — a no-period alias works via `looksLikeSpeaker`'s `aliasToChar.has`). **One citation-allowlist add** — `Publilius Syrus` (the rescued Troilus 3.2 'to be wise and love' maxim). Audit cleanups: the rescue's two All's Well Mars allusions were re-cited from the non-allowlisted Natale Conti's *Mythologiae* → the allowlisted Virgil *Aeneid* / Ovid *Fasti* 3 / Homer *Iliad* 15 (Mars/Ares the war-god); one `the-judgement-of-paris` reference whose kind was `historical` → `classical` (the card lives in classical.json). Ship rates ~93% (All's Well 248/265) / ~96% (Troilus 311/323). **Session 16** (Othello + The Merry Wives of Windsor + Timon of Athens — the Phase-A mop-up, finishing the 23-play drama remainder) confirmed the concurrency rule a **20th, 21st, and 22nd time** (**0 annotator-candidate loss**: Othello 15/15 scenes, Merry Wives 23/23, Timon 17/17; each heavy annotate ran sequentially, overlapped only by its own light 5-agent glossary, with no postprocess/build-index/audit while any annotate was in flight; every pre-postprocess candidate-completeness check passed first time — no judge-file dropouts this session). All three new review queues were cleared by the 3-agent Opus rescue Workflow (**74 rescued, 0 cut**: Othello 22 + Merry Wives 20 + Timon 32 — the citation-fix-not-cut policy held a **16th time**). **NO parser change for any of the three**: Othello and Merry Wives ingest clean, and Timon's sprawling minor cast (apostrophe-label creditors' servants, X-AND-Y unison cues) is handled entirely by exact `characters.json` aliases (the `aliasToChar.has` short-circuit bypasses the length cap and the undotted-unison gate). Two per-play post-ingest text.json cleanups (the documented cosmetic pattern, NOT parser changes): Othello's absorbed Gutenberg 'THE END' footer line was removed (tln_count 3466→3465), and Timon's 5.4 epitaph + Alcibiades's closing speech (17 lines) were reassigned Soldier→Alcibiades (the source marks the change with a `[ALCIBIADES reads the Epitaph.]` SD and no `ALCIBIADES.` prefix). **No new citation-allowlist author** (Pliny/Lucian/Plutarch already in; the new and rescued classical allusions also use Galen/Apollodorus/Aulus Gellius/Boethius, which `citation-check` already accepts). Ship rates ~92% (Othello 326/355) / ~89% (Merry Wives 226/254) / ~83% (Timon 221/266); one post-merge cleanup re-typed an annotator-invented `dramatic_irony` (Othello) → `cultural_context`. **With these three, all 23 plays of the Phase-A remainder ship: the portal now ships 37 of 43 plays / 10,371 annotations / 210 reference cards, every `_review_queue/` empty corpus-wide, and Phase A (the full dramatic canon) is COMPLETE — Phase B (the poetry basket, Sonnets-led) is next.**

---

## Known limitations — parser history (per-play gated normalizers)

Cross-cutting limitations (#1–5: anchor word-granularity, subagent-JSON quoting, gitignored `_*`
files, Windows/Astro glob cache, the unclosed-`[` guard) stay in `CLAUDE.md`. The per-play gated
parser normalizers below are historical detail; each was verified **byte-identical for prior shipped
plays** via the re-ingest regression diff.

- **(6) Henry VI edition normalizer.** The Open Shakespeare **Henry VI** transcription uses a different convention — ordinal/no-period act headers (`ACT FIRST`), scene settings on the line *after* the SCENE header (so `Westminster Abbey.` was misread as a speaker), and many *unbracketed* stage directions (`Dead March. Enter the funeral...`). The **slug-gated** `normalizeHenryVIEdition` in `parse-gutenberg.ts` folds separate-line settings into the SCENE header and wraps an unbracketed-SD block ONLY when its first line is a high-confidence stage cue (Enter/Exeunt/Alarum/Flourish/Dead March/etc.), so a dialogue continuation after an in-speech blank line is never swallowed; the 8 prior plays re-ingest byte-identical. Added `Herodotus` to the citation allowlist (Charles's Rhodope, 1H6 1.6).
- **(7) Inline stage direction at the START of a dialogue line** (`[Aside.] Not I.`, 3H6 4.1; cf. Hamlet's Ghost `[Beneath.] Swear.`): the short reply looks like a speech prefix, and because `emitSD` sets `expectSpeaker`, the trailing words were misread as a phantom speaker and the line lost. Fixed in `parse-gutenberg.ts` (`dispatchText` no longer treats text *after* an inline SD as a possible speaker), **gated on the Henry VI slugs** (`INLINE_SD_GUARD`) so the 10 already-shipped plays re-ingest byte-identical — verified (Hamlet's 4 Ghost 'Swear.' cries are intentionally left as-is; correcting them would shift Hamlet's TLNs and break its shipped annotation anchors). Richard III ingests clean without the guard, so it stays off there.
- **(8) Henry VIII / King John quirks (Session 5, all gated so the 12 prior plays re-ingest byte-identical).** Henry VIII's `THE PROLOGUE.` is followed directly by verse with NO Chorus speaker label (unlike Romeo and Juliet / Henry V), so the buffered prologue had no speaker and would be dropped; fixed by attributing it to a `Prologue` chorus when the play defines a `PROLOGUE.` alias (`hasPrologueSpeaker`), while the closing bare `EPILOGUE` reads as an `Epilogue` chorus through the existing SONG_HEADER-alias path. Henry VIII also occasionally prints an inline speaker label opening a shared verse-line (`NORFOLK. I thank your Grace,`, 1.1) — split off as a new speaker, gated `INLINE_SPEAKER_GUARD = slug === 'henry_viii'`. King John needed a no-dot `CONSTANCE` alias (the source drops the period at line 1622, else her 3.1 speech merges into Elinor's) and a `looksLikeSpeaker` tweak so a unison prefix (≥2 dotted ALL-CAPS known aliases, e.g. `PEMBROKE. and BIGOT.`) is accepted regardless of length — JC's 18-char cap had rejected the 20-char unison (self-gating: requires this play's aliases). Added `Pindar` and `Pausanias` to the classical citation allowlist (the Olympian games, 3 Henry VI 2.3, re-sourced during the rescue sweep). Regression-verified: re-ingesting all 12 prior plays changes only the date stamp (save the documented R2-DUCHESS / H4.1-GLEND post-ingest cleanups, which revert on a naive re-ingest and are restored with `git checkout`).
- **(9) Pericles' per-act Presenter, Gower (Session 6, slug-gated to `pericles`).** Pericles is framed by the medieval poet Gower, who speaks a rhymed chorus before every act — introduced by `[Enter Gower]`, in Act I with NO speaker label and in Acts II-V with a `GOWER.` label — plus mid-act interludes (4.4, 5.2) and the closing Epilogue, several carrying **dumb-shows**. The Romeo/Henry V chorus-buffer machinery was generalized: `emitSD`'s `enter chorus|rumour` trigger now also fires on `enter gower` **only at act-open** (`lastSceneNum === 0`), so the act-opening chorus buffers and prepends to the act head (and the label-less Act I verse is attributed to Gower by presetting `curSpeaker`), while Gower's mid-act / Epilogue appearances stay in their own numbered scene. An unbracketed `DUMB SHOW.` header (Act II) is rendered as a stage direction (it otherwise parses as a 2-word speaker); the other dumb-shows are bracketed `[Dumb Show.]` and need no case. **Gated `slug === 'pericles'`, NOT an alias check** — Henry V (Captain Gower) and Henry IV Part 2 (a messenger Gower) both define a `GOWER.` alias, so an alias-only gate would have wrongly fired on those shipped plays. Typo speaker-variants are aliased in `characters.json` (`PERACLES.`→Pericles, `HELCANUS.`/`HELICANE.`→Helicanus, `BARD.`→Bawd). Regression-verified byte-identical across all 14 prior shipped plays. **The Tempest needed NO parser change** (its masque deity-speakers Iris/Ceres/Juno are pure `characters.json` aliases). This generalized chorus machinery is the hook for Session 7's **Time-as-Chorus** (Winter's Tale 4.1).
- **(10) Titus source-path alias (Session 6 Phase-C fix):** `scripts/lib/paths.ts` now maps the slug `titus_andronicus` → the vendored `tragedy_of_titus_andronicus_gut.txt` via `MODERN_SOURCE_ALIAS`; all other slugs are unchanged.
- **(11) Cymbeline's modern vendored source was INCOMPLETE — repaired from Moby XML (Session 7).** The vendored `texts/gutenberg/cymbeline_gut.txt` (Open Shakespeare) is missing Acts III and IV — the entire Welsh storyline (Belarius's cave, Guiderius/Arviragus, Milford-Haven, the 'Fear no more' dirge, Cloten's beheading): the text jumps straight from 2.5 to 5.1 (and its combined `ACT II. SCENE I.` headers are malformed). Rather than edit the read-only vendor dir (Hard Rule #1), the complete public-domain **Moby Shakespeare XML** (`texts/moby/cymbeline_moby.xml`) was converted to standard `_gut.txt` form by `scripts/pipeline/_moby-to-gut.mjs` (a gitignored `_` helper: maps `<ACT>/<SCENE>/<TITLE>`→headers, `<STAGEDIR>`→`[..]`, `<SPEAKER>`→ALL-CAPS label joining unison speakers, `<LINE>`→text) and committed as `data/sources/cymbeline_gut.txt`; `gutenbergModernPath` returns it via a slug-gated `FULL_SOURCE_OVERRIDE` (one key: `cymbeline`). The converted text uses the standard `ACT I.`/`SCENE I. <setting>` + bracketed-SD + ALL-CAPS-speaker shape, so **no parse-gutenberg change was needed** and all 16 prior shipped plays re-ingest byte-identical (verified — only the date stamp + the documented R2-DUCHESS / H4.1-GLEND reverts differ). If another vendored modern source proves defective, the same Moby→gut path applies (Moby XML exists for every play under `texts/moby/`).
- **(12) The Taming of the Shrew's Christopher Sly INDUCTION — a gated pseudo-act (Session 8, slug-gated to `taming_of_the_shrew`).** Shrew opens with a two-scene Induction (the gulled tinker Sly watching the play) that precedes `ACT I.` and carries its own `SCENE I.`/`SCENE II.` headers — structurally unlike Henry IV Part 2's single-speech Rumour Induction (which buffers into Act 1 via the prologue hook). It is modeled as a **pseudo-act numbered 0**, titled `INDUCTION.`, before Act 1 (Folger/MIT render it 'Induction, Scene 1/2'; TLN 1 begins in it). Required: a one-line schema relaxation (`act.number` minimum 1→0; the 18 prior plays still validate), a content-start that begins at the INDUCTION header, a main-loop branch turning `INDUCTION.` into `pushAct(0, 'INDUCTION.')` (else it passes `looksLikeSpeaker` and is mis-read as a speaker), a one-line `merge-glossary` change (loop from act 0 to pick up `glossary-0.json`), and a small scene-page change labeling act 0 'Induction' (the index page already used `act.title`). Gated `INDUCTION_AS_ACT`; the 18 prior plays re-ingest byte-identical (verified).
- **(13) Love's Labour's Lost + Comedy of Errors edition quirks (Session 8).** LLL: (a) Act 2's only scene is mislabeled `SCENE II.` (no Scene I), which would ship as a phantom 'Act 2, Scene 2' — a **gated sequential within-act scene-renumber** (`SEQUENTIAL_SCENES = slug === 'loves_labours_lost'`) stores 2.1 while the literal header number still drives the missing-ACT-header recovery. (b) Two `@@@`/`@@@@` transcription-junk lines (source 627, 4484) were absorbed as dialogue or consumed the speaker-expectation (swallowing Longaville's line into Berowne's) — a **general** `^@+$` skip (no other vendored source contains `@`, verified by grep, so byte-identical for all shipped plays) drops them without disturbing speaker state. (c) The closing **Spring/Winter songs** (`SPRING`/`WINTER` indented part-labels with no period) and **Moth's `MOTH [Singing.]` Concolinel cue** (a speaker label sharing a line with an inline SD) are handled purely as `characters.json` **no-period aliases** — `SPRING`/`WINTER` map to two song-personification characters, `MOTH` (no dot) to Moth — no parser change; the Nine Worthies pageant and Masque of Muscovites also needed none (character data, like Midsummer's Pyramus). Comedy of Errors needed only `characters.json`: the four identical twins use full disambiguated labels (`ANTIPHOLUS OF SYRACUSE.` etc., 18-23 chars) accepted as exact aliases despite the 18-char `looksLikeSpeaker` cap, plus a no-dot `BALTHAZAR` (source 1042) alias; Arabic scene numbers (`SCENE 1.`) were already handled. Added `Xenophon` + `Diogenes Laertius` to the classical citation allowlist (Socrates' Xanthippe, Shrew 1.2).
- **(14) Antony and Cleopatra: Roman numerals above X + a combined ACT/SCENE header (Session 10, GENERAL — not slug-gated).** A&C is the canon's first play with an act of more than ten scenes (Act 3 has 13, Act 4 has 15) and the first to print a combined act+scene header on one line. Two correctness fixes in `parse-gutenberg.ts`, both general because no shipped play has either shape: (a) `toInt` now converts Roman numerals by subtractive notation (`romanToInt`, returning values identical to the old I..X lookup for 1-10) instead of a 10-entry table that returned 0 for XI-XV — the 0 made `SCENE XI.`-`SCENE XV.` fall through to `emitDialogue`, silently merging those scenes' text into scene X (the first ingest produced only 33 of 42 scenes); (b) a pre-pass `flatMap` splits a combined `ACT n. SCENE m. <setting>` line (A&C's Act 4, whose other four acts use a separate `ACT n.` line) into its two canonical lines so the existing ACT/SCENE paths handle each (including the scene-setting wrap). Proven byte-identical for all 23 prior plays by an **original-vs-patched re-ingest hash comparison**: the usual committed-diff is noisy (the date stamp + the documented R2-DUCHESS/H4.1-CAR/GLEND manual reverts), so instead all 23 were re-ingested under both the original and patched parser on the same day and their `text.json` SHA-256 hashes compared — identical. Speaker handling needed no parser change: the OCR typo `THYMUS.` (3.13)→Thyreus and the `SONG.` Bacchanal (2.7)→a `song` ensemble are `characters.json` aliases, and eleven title-case verse wrap-words stayed dialogue (the Coriolanus `Marcius.` lesson).
- **(15) Much Ado About Nothing + Twelfth Night: gated song-attribution fixes (Session 12, both slug-gated so the 26 prior plays re-ingest byte-identical).** Much Ado's first song 'Sigh no more, ladies' (2.3) is introduced by an `[Balthasar sings.]` stage direction with the lyrics on unlabeled lines, so they were folded into the prior speaker (Benedick). `SINGER_SD_SETS_SPEAKER = slug === 'much_ado_about_nothing'` makes `emitSD` resolve the singer named in a `[<Name> sings]` direction (via a name/de-dotted-alias lookup built from `characters.json`) and set `curSpeaker`, so the lyrics attribute to Balthasar; the second song 'Pardon, goddess of the night' (5.3) has its own `SONG.` prefix → a `song` ensemble. Twelfth Night's closing song 'When that I was and a little tiny boy' (5.1) is cued by a *title-case* `Song.` line (`SONG_HEADER` is case-sensitive ALL-CAPS, so it would emit a stray dialogue line 'Song.' under Feste); `TITLE_SONG_CUE = slug === 'twelfth_night'` treats a standalone title-case `Song.` as a song header (the play's other two songs, 2.3 'O mistress mine' and 2.4 'Come away death', use the all-caps `SONG` already matched). Both proven byte-identical for all 26 prior plays by the original-vs-patched re-ingest hash comparison — crucially including the Tempest (Ariel's `[sings]` songs, untouched because the singer-SD rule is slug-gated) and Macbeth (whose title-case `Song.` is ALIASED to a `song` speaker, so the `!aliasToChar.has` guard skips the TITLE_SONG_CUE branch anyway). The title-case rule is gated rather than a general case-insensitive `SONG_HEADER` precisely to avoid eating the lowercase `air.` (a line of dialogue, not a cue) in the as-yet-unbuilt Two Gentlemen of Verona. Speaker handling otherwise pure `characters.json`: Twelfth Night's Sir Andrew opens with the hyphenated `AGUE-CHEEK.` prefix (his surname, source line 295) — `looksLikeSpeaker`'s shape heuristic rejects the hyphen, so an exact alias was added (else his first speech folded into Sir Toby); Viola-as-'Cesario' is one role, Sebastian a distinct twin, `CLOWN.`=Feste, `DUKE.`=Orsino. Added `Musaeus` (Hero and Leander) and `Heliodorus` (the 'Egyptian thief', TN 5.1, via Underdowne 1569) to the classical citation allowlist.
- **(16) As You Like It + The Two Gentlemen of Verona: gated song + inline-speaker fixes (Session 13, all slug-gated so the 28 prior plays re-ingest byte-identical).** **As You Like It** — 'Blow, blow, thou winter wind' (2.7) is cued by `[AMIENS sings.]` then a `SONG` header with unlabeled lyrics that would fold into the prior speaker (Duke Senior); the `SINGER_SD_SETS_SPEAKER` gate (from Much Ado) is **widened to add `as_you_like_it`**, so the same `[<Name> sings]`→speaker rule attributes them to Amiens. Its two bare-`SONG.` songs (the 4.2 horn song 'What shall he have', the 5.3 pages' 'It was a lover and his lass') go to a `song` ensemble (`characters.json` alias `SONG.`), distinguished from the no-period `SONG` headers (2.5/2.7/5.4) by the trailing period; 2.5 'Under the greenwood tree' and the 5.4 Hymen song attribute via their own `AMIENS.`/`HYMEN.` prefixes, and the Roman-numeral stanza markers `I.`/`II.`/`III.`/`IV.` are caught by the existing `VERSE_NUMERAL` rule. **The Two Gentlemen of Verona** — (a) `SONG_SD_SETS_SINGER = slug === 'two_gentlemen_of_verona'`: the serenade 'Who is Silvia?' (4.2) is cued by a bracketed `[SONG]` whose unlabeled lyrics would fold into the bystander Host (who just said 'let's hear 'em'), so a bare `[SONG]` stage direction sets `curSpeaker` to a `Musicians` ensemble (alias `SONG.`, resolved via `nameToChar`). (b) A slug-gated **inline/bracketed speaker-label splitter** (a pre-pass `flatMap`): this early edition prints the 1.1 'nod/ay/noddy' stichomythia and several asides with the speaker label INLINE on the dialogue line — bracketed (`[SPEED] Ay.`) or dotted (`PROTEUS. Nod, ay?  Why, that's noddy.` / `SPEED. [Aside] ...`); left as-is, `[SPEED]` parsed as a stage direction and the trailing `Ay.` became a phantom 'Ay' speaker (3 lines lost; caught by `_verify-ingest.mjs`'s unmapped-speaker check). The splitter peels a leading **known-alias** label (either form) onto its own line so the standard prefix path attributes it; the alias check means only real speaker labels split, and the lowercase `air.` (2.4 dialogue) and the title-case wrap-words (`Nothing.`, `Banished Valentine.`, etc.) are untouched. All three fixes proven byte-identical for all 28 prior plays by the original-vs-patched re-ingest hash comparison (only the date stamp + the documented R2/H4.1/Titus manual reverts differ). Other speaker handling is pure `characters.json`: Julia-as-'Sebastian' is one `julia` role, Crab the dog a silent non-speaking role, the bare `OUTLAW.` (5.4 'A prize, a prize!') a generic band member alongside the three numbered Outlaws.
- **(17) The Merchant of Venice + Measure for Measure: gated song-SD + undotted-unison fixes (Session 14, both slug-gated so the 30 prior plays re-ingest byte-identical).** **The Merchant of Venice** — its song 'Tell me where is fancy bred' (3.2) is cued by a DESCRIPTIVE bracketed stage direction `[A Song, whilst BASSANIO comments on the caskets to himself.]` (no singer named), unlike Two Gentlemen's bare `[SONG]`; the unlabeled lyrics would fold into the prior speaker (Portia, who has just finished 'Away then! I am lock'd in one of them'). `SONG_SD_DESC_SETS_SINGER = slug === 'merchant_of_venice'` makes `emitSD` route a stage direction beginning 'A Song' to the play's `musicians` ensemble (alias `SONG.`, resolved via `nameToChar`). The Prince of Morocco's multi-word prefix `PRINCE OF MOROCCO.` (+ the mixed-case `PRINCE OF Morocco.` variant at source line 777) is handled purely by exact `characters.json` aliases — `looksLikeSpeaker`'s alias short-circuit (line 214) fires before the `OF` stopword would reject it; `extract-speakers` misses multi-word labels, so the post-ingest unmapped-speaker check (`_verify-ingest.mjs`) is what catches a missing Morocco. **Measure for Measure** — its 5.1 unison cue `ANGELO and ESCALUS.` prints the LEADING name undotted, so the dotted-parts unison test (which requires every part to end in '.') misses it, and the 19-char line then trips the single-name length cap and is read as dialogue, injecting a stray `ANGELO and ESCALUS.` line into the Duke's speech. `UNISON_AND_UNDOTTED = slug === 'measure_for_measure'` adds a gated branch to BOTH `looksLikeSpeaker` (accept the `NAME and NAME.` form before the length cap when both names are known aliases) and `canonical` (join the two proper names → 'Angelo and Escalus'), mirroring the existing dotted-parts unison path that handles JC's `TITINIUS. MESSALA.` / King John's `PEMBROKE. and BIGOT.`. Measure's 4.1 Boy-sung song 'Take, O take those lips away' needed **NO** parser change: the singing Boy is a `boy` character aliased `SONG`/`SONG.` (both the no-period header form and the dotted form), so the all-caps `SONG` header at the song's head — preceded by the SD `[MARIANA discovered sitting; a Boy singing.]` — is recognized as an aliased speaker by the existing line-601 path (the `!aliasToChar.has` guard skips the SONG_HEADER-as-SD branch), attributing the lyrics to the Boy; without the alias the lyrics would have been *dropped* (`curSpeaker` is null at scene-open and `emitDialogue` returns on a null speaker). Both gated fixes proven byte-identical for all 30 prior shipped plays by the original-vs-patched re-ingest hash comparison (only the date stamp + the documented R2/H4.1/Titus manual reverts differ). Other speaker handling is pure `characters.json`: Duke Vincentio = `DUKE.` throughout, even disguised as 'Friar Lodowick'; Pompey the bawd/clown = `CLOWN.` (the title-case 'Pompey.' / 'Mistress Overdone.' answers at 1.2, and the eight other title-case single-word lines flagged by `extract-speakers` — `Amen.`, `Judge.`, `Yea.`, `True.`, `Proof.`, `Right.`, `Mum.`, `Mutually.` — are all in-block dialogue replies sitting under a real ALL-CAPS prefix, so none leaked); Mistress Overdone = `BAWD.`; two distinct friars (`FRIAR.` = Thomas in 1.3, `PETER.` = Peter in 5.1); `CLERK.` is the court clerk, distinct from Nerissa's law-clerk disguise.
- **(18) Troilus and Cressida: the armed bare-`PROLOGUE` + a repeated-`ACT n.` header (Session 15; All's Well needed none).** **Troilus** — its opening 'armed Prologue' has the bare header `PROLOGUE` (no leading 'THE') and the play defines no uppercase `CHORUS.` speaker, so neither existing framing rule matched (`PROLOGUE_HEADER` requires 'THE'; the bare-prologue rule requires `hasUpperChorus`, for Henry V) and the whole Prologue would be skipped as front-matter. Three coordinated **slug-gated** touches fix it: (a) `isFramingHeader` returns true for a bare `PROLOGUE` when `slug === 'troilus_and_cressida'`; (b) the framing-branch prologue-speaker-attribution is broadened to accept `BARE_PROLOGUE_HEADER` for this slug, attributing the buffered verse to a `prologue` character aliased `PROLOGUE.` (WITH the period only — NOT bare `PROLOGUE`, so the framing branch's `!aliasToChar.has(trimmed)` guard stays true for the bare-header line); (c) a slug-gated skip of the redundant `TROILUS AND CRESSIDA` title line this vendored edition prints between the header and the first verse line (else it would be the Prologue's opening line). **Gated on the SLUG, NOT on `hasPrologueSpeaker`, because A Midsummer Night's Dream ALSO defines a `PROLOGUE.` alias** (Quince's play-within prologue) — an alias-only gate would make Midsummer's bare `PROLOGUE` a framing header in the content-start scan (`firstFramingIdx`, which has no `!aliasToChar` guard). Result: the Prologue ('In Troy, there lies the scene...') reads at TLN 1 at the head of 1.1; Pandarus's closing Epilogue is plain dialogue (prefixed `PANDARUS.`, no `EPILOGUE` header). The second fix is **GENERAL** (not slug-gated): this edition REPRINTS the act header before every scene (`ACT I.` appears again before 1.2 and 1.3, etc.), so the parser first produced 24 phantom acts; the ACT_HEADER branch now skips an act header whose number equals the already-open act (`n === currentAct.number`) — byte-identical because no other shipped play repeats an act number. Two **OCR-typo speaker aliases** in `characters.json`: `ANDARUS.` (1.2 source line 476, a dropped-P `PANDARUS.`) → Pandarus, and `AGAME` (1.3 source line 1200, a truncated no-period `AGAMEMNON.`) → Agamemnon — the no-period alias works via `looksLikeSpeaker`'s `aliasToChar.has` short-circuit (the Measure-Boy `SONG` mechanism), and without it Agamemnon's reply 'He hears nought privately...' mis-attributed to Aeneas; both were caught by `_verify-ingest.mjs` (unmapped + bare-ALL-CAPS-leak checks), NOT by `extract-speakers` (which lists corrupted prefixes as distinct labels). `BOY.` (Troilus's page), `MYRMIDON.` (5.7), and `SOLDIERS.` (5.9 unison) are added speakers; `SERVANT.` is shared (Pandarus's + Diomedes's); the 3.1 Pandarus song 'Love, love, nothing but love' is properly prefixed `PANDARUS.` + `[Sings.]` (no song machinery); eight title-case phantoms (`Amen.`, `Not I.`, `Troilus.`, `He.`, `Achilles.`, `From Troy.`, `Thersites.`, `From Troy and Troilus.`) stayed dialogue. **All's Well That Ends Well needed NO parser change** — its 1.3 Clown song 'Was this fair face the cause' is properly prefixed `CLOWN.` then `[Sings.]` (the `SINGER_SD_SETS_SPEAKER` rule is gated to much_ado/as_you_like_it, so it never fires here); its two French lords `FIRST/SECOND LORD.` (doubling as the 2.3 bachelors with a one-line `FOURTH LORD.`), the 3.2 `FIRST/SECOND GENTLEMAN.` (≠ the Act-5 astringer `GENTLEMAN.`), and `ALL.`/`BOTH.` ensembles are all pure `characters.json`; the phantoms `Excellently.`/`France.` and the read-aloud letter signatures stayed dialogue. Both Troilus fixes proven byte-identical for all 32 prior shipped plays by the original-vs-patched re-ingest hash diff (only the date stamp + the documented R2/H4.1/Titus manual reverts differ). Added `Publilius Syrus` to the classical citation allowlist (the rescued Troilus 3.2 'to be wise and love' maxim, 'amare et sapere vix deo conceditur').
- **(19) Othello + The Merry Wives of Windsor + Timon of Athens: NO parser change (Session 16, the Phase-A mop-up).** None of the three needed a parser change. **Othello** and **Merry Wives** ingest clean (standard structure). **Timon**'s sprawling minor cast is handled entirely by exact `characters.json` aliases — the apostrophe-label creditors' servants (`LUCIUS' SERVANT.`, `VARRO'S SERVANT.`, `ISIDORE'S SERVANT.`, `FIRST`/`SECOND VARRO'S SERVANT.`), the X-AND-Y unison cues (`PHRYNIA AND TIMANDRA.`, `BOTH VARRO'S SERVANTS.`), the 3.6 mock-banquet crowd cues (`SOME SPEAK.`, `SOME OTHER.`), the `ALL LORDS.`/`ALL LADIES.`/`ALL SERVANTS.`/`BANDITTI.`/`BOTH.` ensembles, and Othello's `DUKE and SENATORS.` — all recognized because `aliasToChar.has(line)` short-circuits in BOTH `looksLikeSpeaker` and `canonical` (lines 241/273) BEFORE the 18-char length cap and the undotted-unison gate, exactly as the Comedy-of-Errors long twin-labels and Merchant's `PRINCE OF MOROCCO.` are (the exact-alias-bypasses-the-cap precedent — so a sprawling cast is pure character data, not a parser problem). `_verify-ingest.mjs` confirmed 0 unmapped speakers / 0 bare-ALL-CAPS leaks for all three, and the rendered site shows the unison/apostrophe labels as clean names ('First Varro's Servant', 'Phrynia and Timandra', 'Both Varro's Servants'). Distinct roles avoid the consecutive-same-speaker merge: `LUCILIUS` (Timon's servant) ≠ `LUCIUS` (a flattering lord) ≠ `LUCULLUS`; `TITUS` is a creditor's serving-man (NOT the Roman of Titus Andronicus). Two **per-play post-ingest text.json cleanups** (the documented cosmetic pattern — cf. Titus's dangling 'Horns', Antony's stray ']', Richard II's `DUCHESS` reassignment — NOT parser changes): (a) **Othello** — the vendored source ends `[Exeunt.]` + blank lines + `THE END` with no trailing newline, so the parser took `THE END` as Lodovico's final spoken line; the line was removed and `tln_count` corrected 3466→3465. (b) **Timon** — the 5.4 epitaph ('Here lies a wretched corse...') and Alcibiades's closing speech were mis-attributed to the `SOLDIER` because the source marks the speaker change with the stage direction `[ALCIBIADES reads the Epitaph.]` and gives no `ALCIBIADES.` speech-prefix, leaving `curSpeaker` = Soldier; the 17 affected lines were reassigned Soldier→Alcibiades post-ingest (the Richard II `DUCHESS` / Coriolanus reassignment pattern). The Merry Wives Welsh/French **dialect** (Evans/Caius) is handled NOT by the parser but by the annotate/glossary `guidance` arg (gloss/translate the sense; exclude garbled dialect spellings from the glossary — the Henry-V French-passages policy); its 5.5 fairy `SONG.` routes to a `fairies` ensemble via the existing SONG-alias path, and Ford-disguised-as-'Brook' is a dialogue name (prefix stays `FORD.`). No new citation-allowlist author was needed.
